
Growth Watchdog — 2026-09-18 (Friday 17:00 run)

**Metrics** (`scripts/growth-check.mjs`, product `quarterline`, day 18 since launch 08-31):
- `page_view` 161 · `checkout_click` 4 · `export_click` 2 · `agent_query` 0 (quarterline)
- `unique_sessions` 58 (last 7d: 46) · `session_instrumented_rows` 62 · `session_id_column` true
- `gross_revenue_usd` $161 / `charge_count` 9 — **TEST MODE** (`sk_test`, confirmed in repo `.env`)

**Provenance (raw rows, per playbook §5)**
- The +4 sessions since the 08:00 sweep are 4 page_views at 09-18 10:02:20–23 → the Build Watchdog Playwright run. **58/58 sessions all-time are factory-generated.**
- Non-CI sessions unchanged at **5** (`30eb9935` recurring evening session, incl. a `checkout_click` 09-16 23:56; `3b3193ce`, `3f26bb8f`, `49d8211a`, `d8e77f23`). **0 provably-external sessions in 18 days.**
- `agent_query` = 3 all-time, all **FacturGate deploy smoke** (09-17 03:50, `validate_einvoice`×2 + `check_eu_vat_id`), invisible to the quarterline-scoped script → **organic agent demand = 0.**

**Gate verdicts**
- **Day 7 (due 09-07): MISS — held.** pSEO fallback already shipped (`dd1251a`, 9→20 routes). Nothing further.
- **Day 14 (due ~09-14): MISS on both legs — held.** Real revenue $0 (test mode), organic agent queries 0. Fallback (A/B copy test) is **blocked-on-traffic**: there are no external visitors to split.
- **Day 30 (due ~09-30): not yet due** (12 days out). Due date is a Wednesday, so the daily 08:00 sweep will score it — and it **cannot be honestly scored until the internal-traffic marker ships** (approval item 5, still unbuilt).

**Actions taken (autonomous, non-destructive)**
1. Logged this run's gate row + provenance to `skills/self_improvement_eval.md` (commit `0d72585`, pushed).
2. No new fallback fireable and no destructive action — nothing is due for hibernation.

**⚠️ Needs @Simon / founder**
- **Stripe is still test mode** — $161/9 charges are `sk_test`, $0 real. Day-30 inherits this unless answered (approval item 1).
- **Internal-traffic marker (approval item 5)** is now the hard gate: it must land before 09-30 for any honest Day-30 verdict, and it's the only thing that will ever turn "0 provably external" into a real signal.
- **Distribution queue** (item 2): top item's event date is **today**, still 36/36 `ready`, 0 published, untouched since 09-12 — 18 days live with zero external exposure, while the Q3 tax wedge expired unused on 09-15.
