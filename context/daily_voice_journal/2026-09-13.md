# Voice Journal — 2026-09-13

Sweep run 08:00 UTC. Everything below is a live probe from this run unless it names a commit.

## Shipped / changed (since the 09-12 08:00 sweep)

- **No code commits to this repo since `f430f8f` (09-12 15:25).** Tree clean, `main` in sync with
  `origin/main`. The 09-12 record-hygiene batch landed: `5b5681d` integrity audit + record-hygiene SOP,
  `2b2dd59` model tiering, `8a14c30` STATUS heartbeat reconcile, `9fcab6a` dead `/api/auth/editorial`
  route removed, `f430f8f` verify SOP (stale `.next/types` pitfall).
- **PressFlow removal verified in production:** `/pressflow` → **404** on `factory.aichieve.net`.
- **The distribution queue was rebuilt in the other repo — it was not lost.** `kotechile/Editorial-Factory`
  landed `a6ff966` (09-12 14:43) + `7651104` (09-12 14:48): `site/distribution.mjs` + dashboard to-do
  queue, live behind auth (`pressflow.aichieve.net` `/` → 401, `/healthz` → 200, `/api/articles.json` → 200,
  12 published articles). So the 09-12 caveat "the code was deleted, not migrated" is right about the
  Next.js page (`791d6cd:src/app/pressflow/page.tsx`) but wrong about the *feature* — a working
  equivalent exists there. No restore-from-git needed.

## Verified today (live, not journal-derived)

- **Build gate at HEAD `f430f8f` is green:** `npx tsc --noEmit` → 0 errors; `npm run lint` → 0 errors,
  1 warning (`src/middleware.ts:4` unused `_request`); `npm run check:tokens` clean; vitest 28/28.
- **pSEO: 20 presets, all 20 routes HTTP 200 live** (re-verified slug-by-slug, incl.
  `florida-llc-estimated-tax-2026`, `500k-qbi-phaseout-2026`, `side-hustle-gig-tax-2026`).
- `node scripts/growth-check.mjs` → `page_view=123` (quarterline), `unique_sessions=23`,
  `unique_sessions_last_7d=23`, `session_instrumented_rows=23`, `checkout_click=3`, `export_click=2`,
  `charge_count=9`, `gross_revenue_usd=161`, `days_since_first_event=12`, `session_id_column=true`,
  `quarterline_event_rows=128`. Raw table: **249 rows** (factory 105 / quarterline 128 / ledgerlink 16),
  last write 09-12 15:24:14.
- **All 23 quarterline sessions are still factory-generated** (raw-row provenance, clustering at
  CI/deploy timestamps): 09-09 20:52 deploy smoke, 09-10 + 09-11 10:01 Build Watchdog Playwright,
  09-11 17:05 Growth Watchdog Playwright, 09-12 15:21 + 15:24 post-removal verification.
  **External sessions, all-time: 0.**
- 4 rows / 2 sessions on the `factory` directory fall outside every CI cluster
  (`3b3193ce…` 09-09 21:22 → 09-10 00:25, `30eb9935…` 09-10 12:04). No UA or referrer is stored, so they
  are **unattributable** — neither provably external nor provably internal. They are the only candidate
  non-CI traffic ever recorded, and the reason the 09-11 edge-case rule 2 (internal marker at source)
  is still the prerequisite for any honest verdict.
- **Production Stripe is TEST MODE — confirmed from the live deployment, not inferred from `.env`.**
  `POST https://factory.aichieve.net/api/checkout` → HTTP 200,
  `sessionId: "cs_test_a1NyVAsBF0hZ9Meaq4KHhLNN0CqrdJS7SVhi1cAriTF4BxgAP03EOFix59"`,
  `url: checkout.stripe.com/c/pay/cs_test_…`. Corroborated in the DB: all **4 `purchases` rows are
  `cs_test_*`** ($9.00 each, all 2026-09-02, `guest_user`) and the single `subscriptions` row is
  `canceled` (09-02). **Real collected revenue = $0**, and every monetization signal is 11 days stale.
  (The probe created one abandoned test-mode session; the checkout route writes no `events` row — the
  table is still 249 rows.)
- **`agent_query` = 0 rows, all-time** (exact count). The `$0.25/query` tier has still never been hit by
  a real caller.
- **`.well-known/mcp.json` still stale.** Prod (200) is byte-identical to the working tree: advertises only
  `calculate_self_employment_2026` (registered nowhere), omits `calculate_qbi_deduction`,
  `calculate_quarterly_estimate`, `format_article_for_linkedin`, `reconcile_stripe_payout`. Unchanged since
  `4b3abfd`. The 20 pSEO routes are now pushing traffic at this manifest.
- Crons: all 9 active; **0 open incidents** (9 historical, all `closed`). Build Watchdog last ran
  09-12 10:05 `ok` → next today 10:00. Growth Watchdog next **09-18 17:00** (Fri).
- `editorial-factory` Supabase `factory_config.distribution_queue`: **36 items, all status `ready`,
  0 published, 0 deleted**, generated 09-12 14:48 — the queue has never been worked by hand.

## Broke / risks

- **The Day-14 gate is due tomorrow (09-14) and nothing is scheduled to score it.** Criterion
  (playbook §3): ≥$50 gross revenue **or** ≥100 agent queries. Live: **$0 real revenue** (test mode) and
  **0 agent queries** → a guaranteed MISS, and the next evaluator is Fri 09-18, four days late. The §3
  fallback for a missed Day-14 is "run A/B copy test" — which cannot be scored either while there is no
  traffic to split.
- **The binding constraint is distribution, not instrumentation.** 12 days live: 0 external sessions,
  0 agent queries, 0 real revenue — while the Day-7 fallback (20 pSEO routes) has already shipped and is
  indexable. Nothing in the fleet has ever pointed a human or an agent at any of it. Every gate in the
  playbook is unfalsifiable in *both* directions until one external caller exists.
- **Echo outreach: 2 days to the Sept 15 Q3 estimated-tax deadline, still zero execution record**
  (approved 09-09). Only artifact remains `context/growth_blueprints/2026-08-31_quarterline.md`
  (Vectors 1 & 5, copy "ready now"). The tooling that would execute hand-posted distribution now exists —
  in the *other* repo — and its 36 items sit 100 % `ready`/0 published, so nothing is being posted by hand
  anywhere.
- **Day-7 fallback is still an unreviewed artifact.** The 09-11 verdict's two preconditions (tag/exclude
  internal sessions; re-base the window to instrumentation start 09-09 20:52) still do not exist — `events`
  has no internal marker. Tomorrow's Day-14 scoring inherits the same debt and must be done by
  provenance attribution from raw rows (playbook §5, 09-11 rule 1).
- **Measurement flake, recorded so it is not re-escalated:** `growth-check.mjs` reported
  `session_id_column: false` on this sweep's *first* run, then `true` on 5 consecutive re-runs; raw REST
  read confirms the column exists with 56 populated rows. Transient (PostgREST schema-cache/warm-up), not a
  migration regression.
- Journal still has no automated producer (`README.md` says manual) — a skipped sweep is a silent audit gap
  (09-07/09-08 were skipped).

## Founder or fleet notes

- `context/pending_approval.md` is refreshed with today's live state; its 09-12 PressFlow caveat (b) is
  corrected per "Verified today" above (feature rebuilt in `editorial-factory`, only the page was deleted).
- `context/STATUS.md` stale lines corrected this sweep: pSEO "9 presets" → 20, and the `events` table row
  ("live — empty — awaiting traffic") → 249 rows / 0 external sessions.

## Blockers for the sweep to surface

- **[P0 — founder, decision needed today]** The Day-14 revenue gate scores a MISS tomorrow with $0 real
  revenue and 0 agent queries, and no scheduled evaluator (next Growth Watchdog 09-18). Decide: set a live
  `sk_live` key so the gate measures something real, **or** record test mode as intended and let the gate
  score an honest MISS. Either way the consequence is the same: the §3 A/B-copy-test fallback needs traffic
  to measure, so it must be paired with distribution, not scheduled instead of it.
- **[P0 — code, approval needed]** Generate `.well-known/mcp.json` from `src/products/registry.ts` +
  a registry ↔ registered-tool-names ↔ manifest consistency test. Unchanged since `4b3abfd`; re-verified
  byte-identical live today.
- **[P0 — no approval needed, expires 09-15]** Echo: execute GTM Vectors 1 & 5 now. Distribution only.
- **[P1 — code, approval needed]** Tag internal/QA traffic at source on `events` + re-base the gate window
  to 09-09 20:52, so Day-14/Day-30 can be scored without hand attribution.
- **[P2 — record hygiene, fixed in this entry]** Playbook edge-case (2026-09-13) added: a gate whose due
  date falls between watchdog runs must be scored by the daily sweep on the due date; and the transient
  `session_id_column: false`. STATUS.md stale counts corrected.
