# Voice Journal — 2026-09-21

Sweep run 08:00 UTC (Day-21; launch 2026-08-31; Day-14 scored an honest MISS on 09-14; Day-30 due ~09-30,
9 days out — a Wednesday, so the daily sweep scores it and there is no watchdog run that day). Everything
below marked *(live)* was re-probed in this run. Repo changes made this sweep: this entry, the 09-21
growth-gate row, and refreshed `STATUS.md` / `pending_approval.md` lines — docs only, no `src/`.

## Shipped / changed since the 09-20 journal

No product code and no `src/` change in the last day. Three commits, all outside the product build line:

- **`9bbb804` (2026-09-21 06:03:52 UTC — the Weekly Market Recon cron, on time at 06:00, `ok`)** — the
  **first vertical-scoped sweep**. Stage 0 declared `warehouse_automation_robotics_capex` from the ledger's
  never-scanned rotation queue, and the one candidate that cleared the bar is **CaseProof (78)**, PRD at
  `context/recon_proposals/2026-09-21_caseproof.md`, now **queued at the approve gate as item 9**.
  The buyer-side audit of a warehouse-automation business case: the vendor's own quoted numbers re-run
  against fully loaded labour (turnover included), the lines vendors omit (integration, facility,
  maintenance, commissioning ramp), the buyer's tax year (§179 2026 limits $2,560,000 / phase-out
  $4,090,000, 100% bonus, equipment in service by 2026-12-31) and 2–3 competing quotes normalized onto one
  five-year cash model → payback, IRR, NPV at the buyer's hurdle rate, failed-assumption list, sensitivity
  grid, exportable decision pack. Build ≈3.5 h. The free-incumbent check is what shaped it: free *vendor*
  ROI calculators now exist (ISD 2026-07-10, Dexory, Kinexon, KUKA), so a generic calculator is excluded by
  the recon drop rule and the adversarial re-run + multi-quote comparison is the unowned surface.
  Runners-up scored 66 (sustained-throughput validator, shortlist) and 63 (peak-labour vs rented capacity,
  defer — free staffing calculators abound). Run hygiene: vertical recorded in the coverage ledger, pack
  corrected, `node scripts/vertical-sync.mjs --check` exit 0 *(live, re-run this sweep: "26 verticals, no
  drift")*. **Nothing is built; the go/no-go is the founder's.**
- **`1bc3a58` (2026-09-20 13:32:30 UTC) + `ffebf51` (2026-09-20 17:01:45 UTC)** — vertical scoping in this
  repo: `Stage 0` declares a vertical before searching, per-vertical query packs in
  `skills/recon_vertical_packs.md`, a rotation ledger in `context/vertical_coverage.md`, the 26 audience
  verticals vendored from the editorial registry (`context/editorial_verticals.json`, with provenance), and
  `scripts/vertical-sync.mjs` wired into `scripts/verify-build.sh` as `verticals:check`. Both commits
  landed outside any scheduled cron run (nothing is scheduled at 13:32 or 17:01 on a Sunday). Reason on the
  record: 4 of 5 PRDs had landed in one vertical cluster (money/document reconciliation) while the
  archetypes were domain-agnostic — the input scope had drifted, and no field tracked a vertical.

The build line has been idle since ParcelProof shipped (`3584a62`, 2026-09-18 22:05) — **~58 h** at this run.
Reason: items 1, 5, 8 and now 9 are all waiting on the founder, and rule 7 forbids building ahead of the gate.

## Verified today (live, not journal-derived)

- Prod surfaces *(live)*: `/` 200 · `/quarterline` 200 · `/ledgerlink` 200 · `/facturgate` 200 ·
  `/parcelproof` 200 · **all 20 `/quarterline/calc/*`, all 12 `/facturgate/calc/*` and all 6
  `/parcelproof/calc/*` 200** (38 routes, slugs enumerated from the preset files, not sampled) ·
  `/.well-known/mcp.json` 200, **v1.3.0, 8 tools**, sha256
  `6ea29ed1a0a1bac9daf3f9ccf1e4dd4de01a2e4934d4f29b7f4878e37ee2d9c9` — **byte-identical to the working
  tree** (unchanged since 09-18).
- Retired tool name *(live)*: `calculate_self_employment_2026` → **HTTP 400** with the supported list
  (rule 5 holds).
- **Item 8 is unchanged, third day.** (a) The published manifest still advertises the two retired tools,
  `calculate_qbi_deduction` and `calculate_quarterly_estimate`, both attributed `quarterline`;
  (b) `src/app/api/checkout/route.ts:40` still reads
  `app = (plan === "cpa_monthly" || plan === "pdf_audit_export") ? "quarterline" : "factory"`, and a fresh
  `app`-less `pdf_audit_export` probe *(live)* returned `cs_test_a1Ay2uDa5kqbOHjBIiqULWfh9eYBNNI92pCS6…`.
- `node scripts/growth-check.mjs` *(live)* → `page_view=218` (+11), `checkout_click=4`, `export_click=2`,
  `unique_sessions=115` (+11), `unique_sessions_last_7d=88`, `session_instrumented_rows=119`,
  `quarterline_event_rows=224` (+11), `session_id_column=true`, `charge_count=9`,
  `gross_revenue_usd=161`, `days_since_first_event=20`.
- Raw `events` *(live, full read)*: **923 rows** (parcelproof 265 / quarterline 224 / facturgate 187 /
  factory 183 / ledgerlink 64), last write **2026-09-21T02:53:16Z**, first 2026-09-01 19:18:36Z;
  **526 distinct session ids**. **+124 rows since the 09-20 sweep**, and the provenance is 116 CI rows
  (39 = the 09-20 10:00 Build Watchdog; 77 = the two pre-commit verify runs at 13:32 and 17:01), 1 residual
  row at 09-19 23:xx, and **8 rows from one returning browser** — see below.
- **One returning browser is the only visitor-like signal on the board.** Session `30eb9935…` (first seen
  09-10, the session that holds the only `checkout_click` outside the 09-02 window) made **8 new page
  views**: 09-20 at 12:49:16 (`factory`), 12:49:45 (`ledgerlink`), 12:51:23 (`facturgate`), 15:26:54 and
  15:29:47 (`factory`), then 09-21 02:52:15 (`factory`), 02:53:06 (`ledgerlink`), 02:53:16 (`factory`) —
  23 rows lifetime, four products, still no UA/referrer/path stored. It is browsing the directory and
  three products over two days. It also cannot be separated from the owner's own browser without tagging
  at source — the exact gap the internal-traffic marker closes. **0 provably-external sessions in 21 days.**
- Non-CI session inventory *(live)*: **6 standalone ids** (`30eb9935` 23 rows; `3b3193ce` 4 rows,
  09-09 → 09-19; `3f26bb8f`; `49d8211a`; `d8e77f23`; `e41dfb4c`) plus 5 single-row sessions inside seconds
  of the 09-14 10:01 test burst and **198 null-session rows** (85 factory / 105 quarterline / 4 ledgerlink /
  3 facturgate / 1 parcelproof) that carry **all 4 `agent_query`, 3 `checkout_click` and 2 `export_click`
  rows** — a session-less row can never be attributed to a person or a machine.
- **`agent_query` is still 4, all factory ship probes** (`validate_einvoice` ×2 + `check_eu_vat_id`
  09-17 03:50; `audit_carrier_invoice` 09-18 22:07:10). **Organic agent queries: 0 on day 21.** Day-30's
  agent criterion is ≥100.
- **Production is still Stripe TEST MODE** *(live, Stripe API read-back)*: fresh probe →
  `cs_test_a1Ay2uDa…`; all **4** `purchases` rows `cs_test_*` ($9.00, all 2026-09-02); the single
  `subscriptions` row `canceled`. **Real collected revenue = $0.**
- `factory_config.distribution_queue` *(live)*: still **`[]`**, `updated_at` **2026-09-19T22:42:49Z**
  (unchanged for a second day). **0 items have ever been published.**
- Crons *(live)*: 11 jobs (9 active + 2 completed one-shot build jobs), **0 open incidents** (9 ledger
  rows, all `closed`). Weekly Market Recon ran **on time, `ok`, 06:04**. Every other active job's last run
  is `ok` except the two historical fire-claim mislabels (09-15 `enterprise_tech_leadership`, 09-16
  `gpu_hardware`), which next run **09-22** and **09-23** — the two days the unloaded fix matters again.
- **Growth Watchdog** next fires **Fri 2026-09-25 17:00**; its 09-18 row still gates Day-30 on the unbuilt
  internal-traffic marker. Day-30 has **no watchdog run** on the day (09-30 is a Wednesday), so this sweep
  scores it.

## Broke / risks

- **P0 — $0 real revenue, 9 days to the Day-30 gate.** Production has served sandbox checkout pages for
  21 days (`livemode:false`, measured from the API). Day-30 (≈09-30) inherits $0 unless the credential
  lands or sandbox is recorded as intended.
- **P0 — the retirement is still half-applied on the paid and agent surfaces** (item 8, third day
  measured): the default $9 export still names the retired product and its two tools are still on the
  agent menu. One line each, but `src/` work → approval-gated. Customer-facing only once payments go live.
- **P0 — the strongest recon find in three sweeps is waiting at the gate (item 9, CaseProof 78).** Its
  timing window is now (Sep–Oct peak-staffing commitments; 2026-12-31 in-service date for the §179
  write-off). Build is ≈3.5 h once approved; nothing is built.
- **P1 — the agent leg of the gate is 4/4 factory smoke** vs the ≥100 Day-30 criterion. Organic agent
  demand is 0 on day 21 and the only new agent-visible surface since 09-15 is the retirement defect.
- **P1 — both shipped products sit at `beta` with their public launch call unanswered** (FacturGate since
  09-17, ParcelProof since 09-18). LedgerLink is the only `live` product in the directory.
- **P1 — the internal-traffic marker is still unbuilt**, and today's evidence makes it sharper: the one
  returning browser browsed three products across two days, and nothing in the data can say whether that
  was a person. Every gate keeps reading numbers the factory generates.
- **P1 — one open correctness question on the shipped ParcelProof** (UPS AHS-Dimension trigger 96″ as
  published vs 48″ as intended; one line in `src/lib/calc/parcelaudit/surcharges.ts`) — asked 09-18, still
  unanswered.
- **P1 — durable-record step held for a sixth day, still non-structural.** Today's entry and gate row exist
  because this sweep writes them; `context/daily_voice_journal/` still has no producer.
- **P2 — the fire-claim fix is still unloaded** (patch in tree since 09-16; gateway pid `1963330` still the
  2026-09-12 15:13 process). Restart windows: after ~11:30, before 17:00 UTC — never while a cron run is in
  flight. The two mislabelled jobs run 09-22 and 09-23, so a restart before 06:00 on 09-22 removes the
  last visible symptom.

## Founder or fleet notes

- `context/pending_approval.md` remains the authoritative open-work source; this journal is the daily
  audit. Items 3 and 4 are closed; item 2 is closed as resolved-by-owner-removal; **item 9 (CaseProof) was
  opened by this morning's recon**; items 1, 5, 6, 7 and 8 remain open.
- The recon's own Slack payload already asked for the CaseProof decision at ~06:04 today; this sweep repeats
  it as the day's top ask rather than assuming the earlier message was read.
- Unchanged and still true: `/pressflow` 404s here (the feature lives behind auth in `editorial-factory`),
  `HEAD == origin/main`, tree clean, no rule-7 deviation.

## Blockers for the sweep to surface

1. **[P0, founder, ~5 min]** Live payment key in the deploy env **or** record sandbox as intended. Day-30
   (≈09-30, 9 days) inherits the same $0 otherwise.
2. **[P0, @Simon approve]** CaseProof (78) go/no-go — the first candidate from a never-scanned vertical,
   ≈3.5 h to build, once approved. Its window is Sep–Oct staffing plus the 2026-12-31 in-service date;
   doing nothing means the find is re-scanned next quarter.
3. **[P0, founder decision]** Public launch calls for **FacturGate and ParcelProof** (both `beta`) —
   nothing can reach either product until they are launched.
4. **[P0, @Simon approve, code, one line each]** Item 8 — point the default export at a live product (or
   refuse the default when the registry status is `killed`) **and** drop the two retired tools from the
   agent manifest, so the published surfaces stop advertising a retired product.
5. **[P1, @Simon approve, code]** Internal-traffic marker on `events` covering server-side `agent_query`
   rows + re-based gate window — the Growth Watchdog formally gates Day-30 on it, and it is the only way to
   tell whether the returning browser is a person.
6. **[P1, founder, one-line]** Confirm the UPS AHS-Dimension trigger (96″ as published vs 48″ as intended).
7. **[P1, Simon / SOP]** Make the sweep's durable record non-optional: a producer for
   `context/daily_voice_journal/` or an explicit "write the entry + gate row + commit" step.
8. **[P2, operator, Hermes fork]** Load the fire-claim fix — restart `hermes_cli.main gateway run` while no
   cron run is in flight, ideally before 06:00 on 09-22.
