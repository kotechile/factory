# Voice Journal — 2026-09-20

Sweep run 08:00 UTC (Day-20; launch 2026-08-31; Day-14 scored an honest MISS on 09-14; Day-30 due ~09-30,
10 days out — a Wednesday, so the daily sweep scores it). Everything below marked *(live)* was re-probed in
this run. Repo changes made this sweep: this entry, the 09-20 growth-gate row, and refreshed `STATUS.md` /
`pending_approval.md` lines — docs only, no `src/`.

## Shipped / changed since the 09-19 journal

Two owner workstreams landed overnight, both **by hand (Jorge Fernandez), not by a fleet build**:

**A. software-factory-core — QuarterLine retired, payments generalized.** `bc3d556` (2026-09-19 23:43:27 UTC,
`feat(factory): retire QuarterLine in registry and generalize Stripe checkout & webhooks`):

- `src/products/registry.ts`: **QuarterLine status `live` → `killed`**, description now "Retired from active
  inventory following the Q3 2026 tax deadline window." (LedgerLink stays `live`; FacturGate and ParcelProof
  stay `beta`.) The route `/quarterline` and its 20 `/quarterline/calc/*` presets are untouched — *(live)*
  `/quarterline` still 200 and the showcase renders the retirement copy.
- `src/app/api/checkout/route.ts` + `src/app/api/webhooks/stripe/route.ts`: both hardcoded to QuarterLine and
  are now product-generic — `app` is read from the request, line-item name/description/currency/amount/
  line-items/metadata are caller-supplied or registry-derived, success/cancel URLs follow the product path,
  and the webhook receipt email is templated on the product name. **One residual default:**
  `app = (plan === "cpa_monthly" || plan === "pdf_audit_export") ? "quarterline" : "factory"`, i.e. the default
  $9 export still resolves to the **retired** product (measured live, see below).
- `scripts/growth-check.mjs`: `PRODUCT` is no longer hardcoded — `process.argv[2] || GROWTH_PRODUCT ||
  "quarterline"`. This is a **partial** answer to the long-open item 5 (the script could not count FacturGate,
  ParcelProof or LedgerLink events); the internal-traffic marker itself is still unbuilt.

`HEAD == origin/main == bc3d556`, tree clean, and the deploy landed: the showcase serves the retirement copy.

**B. editorial-factory — pipeline wiped and re-grounded on real search demand.** Six commits
2026-09-19 23:09 → 2026-09-20 01:23: `b927ea6` + `8ef1794` add verticals (Enterprise AI for CIOs, Home &
Lifestyle, and 6 for giniloh.com) with personas and distribution routes; `fbc3606` (23:54:42)
`chore(cleanup): remove all legacy drafts and published articles to start fresh`; `8578c4c`→`1d86a74` wire
Google Search Console (multi-property, dynamic 28-day window, scoring) into the topic engine.

Consequence *(live)*: `published/` holds only `.gitkeep`, `pressflow.aichieve.net/api/articles.json` → `[]`,
`/healthz` → 200, and **`factory_config.distribution_queue` is now an empty list `[]`, `updated_at`
2026-09-19T22:42:49Z`** — the 36 prepared posts from 09-12 were **deleted, not published** (0 ever published).
The sweep's standing P0 "work the queue" has been answered by removal; the honest residue is that 36 finished
posts produced zero distribution and the third wedge (2026-09-18 customs story) expired unused.

## Verified today (live, not journal-derived)

- Prod surfaces *(live)*: `/` 200 · `/quarterline` 200 · `/ledgerlink` 200 · `/facturgate` 200 ·
  `/parcelproof` 200 · `/.well-known/mcp.json` 200, **v1.3.0, 8 tools**, sha256
  `6ea29ed1a0a1bac9daf3f9ccf1e4dd4de01a2e4934d4f29b7f4878e37ee2d9c9` — **byte-identical to the working tree**
  (unchanged since 09-18).
- **The retirement is not complete on two live surfaces.** (a) The manifest still advertises the two retired
  tools, `calculate_qbi_deduction` and `calculate_quarterly_estimate` (both attributed `quarterline`), to
  agents. (b) A default checkout request (`plan: "pdf_audit_export"`, no `app`) still produces a session whose
  metadata is `app=quarterline, appName=QuarterLine` — confirmed by reading the session back from the Stripe
  API, not inferred from source.
- `node scripts/growth-check.mjs` *(live)* → `page_view=207` (+17), `checkout_click=4`, `export_click=2`,
  `unique_sessions=104` (+17), `unique_sessions_last_7d=81`, `session_instrumented_rows=108`,
  `quarterline_event_rows=213` (+17), `session_id_column=true`, `charge_count=9`, `gross_revenue_usd=161`,
  `days_since_first_event=19`.
- Raw `events` *(live, full read)*: **799 rows** (quarterline 213 / parcelproof 208 / factory 169 /
  facturgate 153 / ledgerlink 56), last write **2026-09-19T23:42:28Z**, first 2026-09-01 19:18:36Z; 445
  distinct session ids. **All 166 rows since the 09-19 sweep are CI**: the 10:01 Build Watchdog run (40 rows,
  including the 4 quarterline sessions that alone account for the +17 product-scoped sessions) and the
  owner's pre-commit verify run (124 rows, 23:36–23:42, across all five products). Nothing has been written
  since 23:42:28Z.
- **`agent_query` is still 4, all factory deploy smoke** (`validate_einvoice` ×2 + `check_eu_vat_id` 09-17
  03:50; `audit_carrier_invoice` 09-18 22:07:10), `session_id` null. **Organic agent queries: 0 on day 20.**
- **Non-CI unattributable sessions: 7 session ids (was 5)** — `3b3193ce` (returning browser, 4 × factory
  page_view 09-09 → **09-19 23:28:04**), `30eb9935`, `3f26bb8f`, `49d8211a`, `d8e77f23`, `1199d4ed`
  (09-13 10:08 — outside the 09-13 10:01:35 burst, so counted today where earlier sweeps did not),
  **`e41dfb4c` (new, 09-19 21:50:57, factory)** — plus 16 rows that carry no session id at all. None stores a
  UA/referrer/path → **still 0 provably-external sessions in 20 days live**. They cannot be separated from real
  visitors without tagging at source: `1199d4ed` sits 7 minutes after the 09-13 10:01 Build Watchdog run (a
  plausible untagged CI residual) while `e41dfb4c` and `3b3193ce`'s return are standalone evening views that
  nothing distinguishes from a human — the exact reason the internal-traffic marker is needed.
- **Production is still Stripe TEST MODE** *(live, Stripe API)*: fresh probe → `cs_test_a1MlUGMq…`
  (`livemode: false` — measured from the API this sweep, not inferred from the key prefix); all **4**
  `purchases` rows `cs_test_*` ($9.00, all 2026-09-02); the single `subscriptions` row `canceled`.
  **Real collected revenue = $0.**
- `factory_config.distribution_queue` *(live)*: **`[]` — empty**, `updated_at` **2026-09-19T22:42:49Z**
  (was 36/36 `ready`, 0 published, frozen at 2026-09-12T14:48:22Z for seven days). **0 items have ever been
  published.**
- Crons *(live)*: 11 jobs (9 active + 2 completed one-shot build jobs), **0 open incidents** (9 ledger rows,
  all `closed`). Every active job's last run is `ok` except the two historical fire-claim mislabels
  (09-15 `enterprise_tech_leadership`, 09-16 `gpu_hardware`). No new mislabel on 09-17/18/19 — and none was
  possible on 09-20 for those two, which next run 09-22/09-23.
- **Growth Watchdog** next fires **Fri 2026-09-25 17:00**; its 09-18 row still gates Day-30 on the unbuilt
  internal-traffic marker. The Day-30 gate has **no watchdog run** on the day (09-30 is a Wednesday), so this
  sweep scores it.

## Broke / risks

- **P0 — $0 real revenue, 10 days to the Day-30 gate.** Production has served sandbox checkout pages for 20
  days; the payment generalization does not change that (`livemode: false`, measured). Day-30 (≈09-30)
  inherits $0 unless the credential lands.
- **P0 — the retirement is half-applied on paid and agent surfaces.** The default $9 export still names the
  retired product, and its two tools are still on the agent menu. Both are one-line changes, but changing
  them is `src/` work and therefore approval-gated. Risk is customer-facing only when payments go live.
- **P0 — 36 prepared posts were deleted unposted.** Distribution now has no queue at all; the editorial
  engine will regenerate against search demand, so no action is owed from this repo, but the series stands at
  0 published items after 20 days.
- **P1 — the agent leg of the gate is still 4/4 factory smoke**, and Day-30's agent criterion is ≥100 agent
  queries. Organic agent demand is 0 on day 20.
- **P1 — both shipped products sit at `beta` with their public launch call unanswered** (FacturGate since
  09-17, ParcelProof since 09-18); LedgerLink is the only `live` product left in the directory. Nothing can
  reach FacturGate or ParcelProof while they are unlaunched.
- **P1 — the internal-traffic marker is still unbuilt**, and `growth-check.mjs` now accepting a product
  argument does not substitute for it: the marker must tag events *at source* and also cover server-side
  agent rows, or every gate keeps reading numbers the factory generates.
- **P1 — one open correctness question on the shipped ParcelProof** (UPS AHS-Dimension trigger 96″ as
  published vs 48″ as intended; one line in `src/lib/calc/parcelaudit/surcharges.ts`) — asked 09-18, still
  unanswered.
- **P1 — durable-record step held for a fifth day, still non-structural.** Today's entry and gate row are
  written because this sweep writes them; `context/daily_voice_journal/` still has no producer.
- **P2 — the fire-claim fix is still unloaded** (patch in tree since 09-16; gateway pid `1963330` still the
  2026-09-12 15:13 process). Restart windows: ~10:00–11:00, after ~11:30, before 17:00 UTC — never while a
  cron run is in flight (the Build Watchdog runs 10:00 and the Growth Watchdog 17:00 Fri).

## Founder or fleet notes

- `context/pending_approval.md` remains the authoritative open-work source; this journal is the daily audit.
  Items 3 and 4 are closed; the distribution half of item 2 is **resolved by owner action (queue deleted
  09-19 22:42)**; items 1, 5, 6, 7 remain open, plus the new half-applied-retirement item.
- Unchanged and still true: `/pressflow` 404s here (the feature lives behind auth in `editorial-factory`),
  0 open incidents, no rule-7 deviation, `HEAD == origin/main`, tree clean.

## Blockers for the sweep to surface

1. **[P0, founder, ~5 min]** Live payment key in the deploy env **or** record sandbox as intended. Day-30
   (≈09-30, 10 days) inherits the same $0 otherwise.
2. **[P0, founder decision]** Public launch calls for **FacturGate and ParcelProof** (both `beta`) — nothing
   can reach either product until they are launched.
3. **[P0, @Simon approve, code, one line]** Point the default export at a live product (or refuse the default
   when the registry status is `killed`) **and** drop the two retired tools from the agent manifest, so the
   published surfaces stop advertising a retired product.
4. **[P1, @Simon approve, code]** Internal-traffic marker on `events` covering server-side `agent_query`
   rows + re-based gate window — the Growth Watchdog formally gates Day-30 on it.
5. **[P1, founder, one-line]** Confirm the UPS AHS-Dimension trigger (96″ as published vs 48″ as intended).
6. **[P1, Simon / SOP]** Make the sweep's durable record non-optional: a producer for
   `context/daily_voice_journal/` or an explicit "write the entry + gate row + commit" step.
7. **[P1, operator, Hermes fork]** Load the fire-claim fix — restart `hermes_cli.main gateway run` while no
   cron run is in flight.
