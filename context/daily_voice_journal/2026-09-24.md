# Voice Journal — 2026-09-24

Sweep run 08:00 UTC (Day-24; launch 2026-08-31; Day-14 scored an honest MISS on 09-14; Day-30 due ~09-30,
**6 days out** — a Wednesday, so the daily sweep scores it and no watchdog run exists that day; the Growth
Watchdog does run Fri 09-25 17:00, the day before the weekend). Everything below marked *(live)* was re-probed
in this run. Repo changes made this sweep: this entry and refreshed `STATUS.md` / `pending_approval.md` header
lines — docs only, no `src/`.

## Shipped / changed since the 09-23 journal

**Nothing was built or shipped.** `HEAD == origin/main == b75615e` (the 09-23 sweep's own docs commit), no
commit in any factory repo in 24 h, and the running app image is
`af8yqbwrrnyyfgs9wcg0intj:b75615e8f879…` *(live, `docker ps`)*, i.e. the docs commit itself deployed. The build
line has now been idle since the two 09-22 builds (≈54 h).

Four measurable changes, none of them from a factory build:

- **Traffic: 66 new rows, and 63 of them are our own test run.** `events` **1419 → 1485 rows** *(live, full
  read)*; first 2026-09-01T19:18:36Z, last **2026-09-23T14:11:42Z** — **no event at all in the 18 hours before
  this sweep**. 63 rows landed 10:01:29 → 10:01:45Z on 09-23: the daily Build Watchdog's Playwright run
  (caseproof / facturgate / parcelproof / ledgerlink / factory page views + clicks). The remaining **3 rows are
  one session**, `59a27597…`, which first appeared 09-23 01:39 and came back three times on 09-23: 12:13:50Z,
  12:16:50Z and 14:11:42Z, every one a `page_view` on the factory page and nothing else. It is one of the five
  standalone sessions flagged yesterday and it is **still unattributable** — no user-agent, no referrer is
  stored, so it cannot be separated from the owner's own browser (item 5).
- **`product=caseproof` 114 → 137 rows** *(live)*, i.e. +23, all of it the 10:01 Build Watchdog run. Fourth
  consecutive day the number is our own verification traffic.
- **The written-and-waiting social queue is no longer empty.** `factory_config.distribution_queue`
  *(live)*: **15 items, all `ready`, 0 published**, `updated_at` **2026-09-23T13:18:00Z** (was `[]` for four
  days). The 15 cover six editorial articles (`mcp-skills-extension`, `ai-price-volatility…`,
  `nvidia-hugging-face-moat`, `rollback-breaks-execution-continuity`, `temporal-durable-execution-12-55b`,
  `unfi-warehouse-automation-capex`) across Reddit communities and LinkedIn. No factory cron produces this
  queue, so it was seeded by hand (the owner's stated 09-22 to-do list); **nothing has been posted from it**.
- **Editorial side shipped six articles and a dashboard fix, all by the owner, not this factory** *(live)*:
  `pressflow.aichieve.net` container image `nstjdswcf5p9xckwja89o6z0:2c6f89f…` == its HEAD (`2c6f89f`, "label
  library rows PUBLISHED / DRAFT"); `/healthz` 200 and `/api/articles.json` returns the six published pieces.

## Verified today (live, not journal-derived)

- Prod surfaces on `apps.giniloh.com` *(live)*: `/` **307** → `https://giniloh.com` · `/showcase`,
  `/ledgerlink`, `/facturgate`, `/parcelproof`, `/caseproof`, `/quarterline`, `/embed/countdown` all **200** ·
  legacy `factory.aichieve.net` still **503** on `/`. No regression from the 09-23 migration.
- Manifest *(live)*: `/.well-known/mcp.json` **200, v1.5.0, 9 tools**, sha256
  `2843f352f28fbf1adae82c9fd6ad3808932f99e23043e210c4aa0853acf232ea` — **byte-identical** to
  `public/.well-known/mcp.json` in the tree; `endpoint` `https://apps.giniloh.com/api/agent/calculate`.
- Paid surface *(live, POST — the route is POST-only, a bare GET now returns 405 by design)*:
  `{plan:pdf_audit_export, app:ledgerlink}` → **200** `https://checkout.stripe.com/c/pay/cs_test_a1OHmVri…`
  (sandbox) · `app=caseproof` → **200** `cs_test_a1ppCrNj…` (sandbox) · app-less → **400** naming the inventory
  · `app=quarterline` → **400** "retired and cannot be purchased. Products in inventory: ledgerlink, facturgate,
  parcelproof, caseproof." · `/api/portal` → **307** `?error=no_customer_found`. **Item 8 still holds.**
- **P0 — the live payment key slot still holds a key ID, and Stripe still rejects it.** Read-only
  `GET https://api.stripe.com/v1/balance` with the deployed value → **HTTP 401**, verbatim: *"Invalid API key
  provided: mk_1UAeGx***************wbFj. This looks like the ID of an API key rather than the key itself."*
  The deploy env *(live, container read)* carries the full pair for both modes —
  `STRIPE_MODE=test`, `STRIPE_SECRET_KEY_TEST=sk_test_51…`, `STRIPE_WEBHOOK_SECRET_TEST=whsec_g6Jc…`,
  `STRIPE_SECRET_KEY_LIVE=mk_1UAeGxJaTDc3aAp0lG3UwbFj`, `STRIPE_WEBHOOK_SECRET_LIVE=whsec_vn7B…` — so the only
  wrong value is the one that matters: the live **secret**. Nothing has changed on item 1 in 24 h, and the
  Day-30 gate (≈09-30) inherits $0 until the real `sk_live_…` value is pasted.
- Raw `events` *(live, full read)*: **1485 rows** (parcelproof 459 / facturgate 309 / quarterline 228 /
  factory 225 / caseproof 137 / ledgerlink 127), **899 distinct session ids**, 198 null-session rows, 4
  `agent_query`. `growth-check.mjs` *(live)*: `stripe_mode "test"`, `revenue_is_sandbox true`,
  `gross_revenue_usd 161`, `charge_count 9`, quarterline `unique_sessions_last_7d` **86 → 69** (the window
  rolled forward past the 09-17/09-18 cluster and nothing new replaced it).
- **`agent_query` is still 4** — facturgate ×3 (09-17 03:50) + parcelproof ×1 (09-18 22:07), every one a
  factory ship probe. **Organic agent queries: 0 on day 24.** Day-30's criterion is ≥100.
- **No real revenue** *(live, API read-back + DB)*: 4 `purchases` rows all `cs_test_*` ($9.00, all 2026-09-02);
  the single `subscriptions` row `canceled`. **$0 collected, 24 days in.**
- Crons *(live)*: 30 jobs, **0 open incidents** (9 historical, all closed); `Full Pipeline: agentic_ai` ran
  09-24 06:14 ok, `supply_chain` 09-24 07:09 ok, `home_ops_execution` 09-24 07:38 ok — the editorial fleet is
  healthy and was not touched by this sweep.
- `node scripts/vertical-sync.mjs --check` *(live)* → exit 0, "26 verticals, no drift (vendored)".
- **Domain-migration leftovers unchanged** *(live)*: `/.well-known/ai-plugin.json` is served 200 but its
  `api.url` is `https://factory.aichieve.net/openapi.json` and its `legal_info_url` is the dead host; its
  companion `/openapi.json` 404s; `/robots.txt` and `/sitemap.xml` still 404 with **no producer in the repo**
  (`src/app/robots.ts` / `sitemap.ts` absent, nothing under `public/`) for 44 indexable landing pages.
- Repo *(live)*: `HEAD == origin/main == b75615e`, tree clean except the pre-existing untracked `.hermes/`
  scratch dir.

## Broke / risks

- **P0 — the live key slot is still wrong, now 6 days from Day-30.** Re-measured, not inferred. One paste of
  the real `sk_live_…` (+ `STRIPE_MODE=live`) is the whole fix; until then no sale can succeed.
- **P0 — three finished products are invisible to the public** (all `beta`): FacturGate since 09-17 (7 days),
  ParcelProof since 09-18 (6 days), CaseProof since 09-22 (2 days). No commit touched their status.
- **P0 — the retirement still leaves three QuarterLine surfaces public (item 10).** Unchanged and re-verified:
  `/quarterline` renders the working calculator whose $9 export returns an explicit 400, its 20 preset pages
  are served and indexable, `/embed/countdown` links there.
- **P1 — the Day-30 agent leg is 4/4 factory smoke** vs the ≥100 criterion; organic agent demand 0.
- **P1 — the internal-traffic marker (item 5) is still unbuilt**, and today adds evidence on the other side:
  63 of 66 new rows were ours, and the 3 that were not belong to a session that may still be the owner.
  Without tagging at source, every growth gate reads numbers we generate.
- **P1 — the ParcelProof correctness question is now 7 days open** (UPS AHS-Dimension trigger 96″ as published
  vs 48″ as intended; one line in `src/lib/calc/parcelaudit/surcharges.ts`).
- **P1 — the durable-record step held for a ninth day, still non-structural** (item 6): today's entry exists
  because this sweep writes it. `context/daily_voice_journal/` still has no producer.
- **P2 — item 11 leftovers** (dead-host descriptor + no crawler map) unchanged; **items 7** (gateway restart,
  no visible symptom) unchanged.
- **New observation, not a defect:** the 15 waiting social posts will go stale — they reference 09-21…09-23
  news and none has been posted. If they are not published within days, the editorial side has to re-seed.

## Founder or fleet notes

- `context/pending_approval.md` remains the authoritative open-work source; this journal is the daily audit.
  Items 2, 3, 4, 8 and 9 are closed; **open: 1 (live key), 5 (traffic marker), 6 (durable record), 7 (gateway
  restart, P2), 10 (QuarterLine surfaces), 11 (migration leftovers)** — plus the three launch calls and the
  ParcelProof one-liner.
- Every queue lead is now an owner decision: one paste, three launch approvals, one publish/retire approval,
  one data confirmation. The fleet has nothing buildable ahead of those.
- The owner is working the stack himself — six editorial articles published, the distribution queue seeded,
  the pressflow dashboard fixed, all in the last 48 h. The sweep's job is to measure, which is what this entry
  does; nothing here was built or shipped outside the gate.
- Unchanged and still true: `/pressflow` 404s on the factory host (it lives at `pressflow.aichieve.net`), no
  rule-7 deviation, and every number in this entry is a live probe or a named commit.

## Blockers for the sweep to surface

1. **[P0, founder, one paste]** The live key slot holds the key's **ID** (`mk_…`) — Stripe rejects it with 401
   (re-verified today). Paste the real `sk_live_…` value and set `STRIPE_MODE=live`, or record practice mode as
   intended. 6 days to Day-30.
2. **[P0, founder, one reply]** Launch calls for **FacturGate, ParcelProof and CaseProof** —
   `@Simon approve launch FacturGate, ParcelProof, CaseProof`.
3. **[P0, owner decision]** Item 10 — the three public QuarterLine surfaces.
   `@Simon approve retire QuarterLine pages`.
4. **[P1, founder, one line]** Confirm the UPS AHS-Dimension trigger (96″ as published vs 48″ as intended) —
   7th day open.
5. **[P1, @Simon approve + owner migration]** Internal-traffic marker on `events` + a re-based gate window
   (item 5). The Growth Watchdog formally gates Day-30 on it.
6. **[P1, Simon / SOP]** Make the sweep's durable record non-optional (item 6); 9th day it survives only
   because a sweep run writes it.
7. **[P2, @Simon approve, code]** The migration leftovers (item 11): repoint `ai-plugin.json` off the dead host
   and add a crawler map (`robots.txt` / `sitemap.xml`).
8. **[P2, operator]** Restart the Hermes gateway when no cron run is in flight to load the fire-claim fix
   (no visible symptom left). Do not do it 10:00 or 17:00 Fri, or 06:00–08:30 while the editorial jobs run.
9. **[P2, owner/editorial]** 15 social posts are ready and none is published; the underlying news is 09-21…09-23
   and will age out within days.
