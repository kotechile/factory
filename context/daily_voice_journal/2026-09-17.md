# Voice Journal — 2026-09-17

Sweep run 08:00 UTC (Day-17; launch 2026-08-31; Day-14 scored an honest MISS on 09-14; Day-30 due ~09-30).
Everything below marked *(live)* was re-probed in this run. Repo changes made this sweep: this entry, the
09-17 growth-gate row + one failure-log row, one playbook edge-case, and corrected `STATUS.md` /
`pending_approval.md` lines — docs only, no `src/`.

## Shipped / changed since the 09-16 journal

Five commits, all 2026-09-17 03:26–03:49 UTC — the build line woke after eight idle days:

- `a57539f` fix(webmcp): `.well-known/mcp.json` is now **generated** from `src/products/registry.ts`
  (+ `WEBMCP_TOOL_SUMMARIES`) with a 7-assertion guard test — closes approval-queue item 3.
- `7aad4d7` docs: queue item 3 closed, `skills/webmcp_integration.md` hardened.
- `7134b8e` docs: **FacturGate approved by owner instruction 2026-09-17** (queue item 4);
  **ParcelProof shortlisted**, not started.
- `e77db64` feat(facturgate): **FacturGate v1 beta shipped** — 65 implemented rule ids, 3 metered WebMCP
  tools, 12 pSEO presets, 25 known-answer vectors (60 vitest total); `verify-build.sh` green end to end.
- `c1b3139` docs: `context/STATUS.md` record + two SOP patches.

Deployed image tag = `c1b31393d1fc…` (container up 4 h) → the pushed commit *is* the deployed artifact;
`HEAD == origin/main == c1b3139`, tree clean. Author of record for all five: cron job `0da7fd6c8f68`
("Build: FacturGate (v1, approved 09-17)", ran 03:31→03:51).

## Verified today (live, not journal-derived)

- Prod surfaces: `/` 200 · `/facturgate` 200 (30,770 bytes) · **all 12** `/facturgate/calc/*` 200 ·
  `/ledgerlink` 200 · `/quarterline` 200 · sampled quarterline pSEO
  (`california-freelancer-tax-2026`, `new-york-schedule-c-qbi`, `texas-1099-estimated-tax`) 200.
- `.well-known/mcp.json` *(live)*: 200, sha256 `f3b4cbd26198491de6125e3f28fba084ad568d90efbef619ccec0398bf327fea`
  — **byte-identical to the working tree**, v1.2.0, advertising all six real tools. Queue item 3 is
  verified closed end-to-end. `POST /api/agent/calculate` with the retired name
  `calculate_self_employment_2026` → **HTTP 400** + `supportedTools` list (rule 5 holds live).
- **`agent_query` is no longer 0 — and its first three rows are the factory's own deploy smoke.**
  *(live, exact read)* 3 rows, all `facturgate`, all 2026-09-17 03:50:21–03:50:22 (`validate_einvoice` ×2,
  `check_eu_vat_id` ×1), `session_id` null. The build job's report states it plainly: "my 3 `agent_query`
  rows are deploy smoke, not organic traffic". **Organic agent queries remain 0**, and the metered route
  writes no internal marker, so the agent leg of the Day-30 gate is now polluted by our own ship smoke.
- `node scripts/growth-check.mjs` *(live)* → `page_view=153`, `checkout_click=4`, `export_click=2`,
  `unique_sessions=50`, `unique_sessions_last_7d=46`, `session_instrumented_rows=54`,
  `quarterline_event_rows=159`, `session_id_column=true`, `charge_count=9`, `gross_revenue_usd=161`,
  `days_since_first_event=16`.
- Raw `events` *(live)*: **353 rows** (quarterline 159 / factory 130 / ledgerlink 37 / facturgate 27);
  last write **2026-09-17 06:03:19**. 128 sessions sit inside CI clusters (deploy smoke, Build/Growth
  Watchdog Playwright, the 09-17 verify runs at 03:26 and 03:46–03:47).
- **Non-CI sessions: 4** (was 2 at the 09-16 sweep) — every one unattributable, because no row stores a
  UA/referrer/path: `3b3193ce` (3 factory views, 09-09 21:22 → 09-10 00:25), `30eb9935` (15 rows across
  factory + quarterline + ledgerlink, 09-10 12:04 → **09-16 23:57**; 3 `reconcile_click` and now
  **1 `checkout_click` at 09-16 23:56:36 — the first checkout click outside the 09-02 window**),
  `3f26bb8f` (1 factory view, 09-13 20:34), `49d8211a` (1 factory view, **09-16 19:06:50**, new).
  `30eb9935` is a returning-browser pattern (factory → quarterline → checkout → back, 23:55–23:57 on
  09-16) and the strongest external-traffic candidate recorded to date — **still not provable**. Honest
  form: **0 provably-external sessions in 17 days live**.
- **Production is still Stripe TEST MODE** *(live)*: `POST /api/checkout` → 200,
  `cs_test_a1jLaDVqFx7DXRATpwSVoCwciLaVSLcwhLMDoDraIWJdUYljfJpFzVcreq`; DB: all **4** `purchases` rows
  `cs_test_*` ($9.00, all 2026-09-02), the single `subscriptions` row `canceled` (09-02).
  **Real collected revenue = $0**, and the new 4th `checkout_click` produced no charge (test mode, as
  designed). Every monetization signal is 15 days stale.
- `factory_config.distribution_queue` *(live)*: **36 items, all `ready`, 0 published, 0 deleted**,
  `updated_at` still **2026-09-12T14:48:22Z** — five days untouched. **One queued item carries
  tomorrow's deadline**: the top of the queue is the CBP Form 5106 / IOR-voidation story whose event
  date is **2026-09-18**. The Q3 estimated-tax wedge expired 09-15; the queue's own next item goes stale
  tomorrow with nothing posted.
- Crons: 9 jobs active, **0 open incidents** (9 ledger rows, all `closed`). Build Watchdog next today
  10:00; Growth Watchdog next **Fri 09-18 17:00**.
- Fire-claim fix: the patch **is** in the tree (`cron/jobs.py::heartbeat_fire_claim` no longer wraps
  `_fire_job_lock`; verified by reading the source), but the **gateway process is still the one started
  2026-09-12 15:13**, so the last mislabel is 09-16 08:07 (this job's own run) and `hermes cron list`
  still shows three jobs' last runs as `Interrupted by shutdown`. **No mislabel today**: both 06:00
  Full-Pipeline runs are `completed` + `delivered`. Operator action unchanged — restart the gateway while
  no cron run is in flight.

## Broke / risks

- **P0 — distribution is still the binding constraint, and the queue's own clock is about to expire
  again.** 36/36 `ready`, 0 published, untouched since 09-12 14:48; the 09-15 wedge is gone and item #1
  (`r/supplychain` CBP IOR post) is dated **09-18**. This is the only P0 that needs no approval and no
  code, and it is the reason every gate stays unscoreable.
- **P0 — $0 real revenue, 13 days to the Day-30 gate.** Production has now served test-mode checkout
  pages for 17 days; the 4th `checkout_click` (09-16 23:56) went to a `cs_test_` session that cannot be
  paid. Day-30 (≈09-30) inherits the same $0 unless `sk_live` is answered.
- **P1 — the agent leg of the gate is now polluted by factory smoke.** Three `agent_query` rows exist and
  every one is our own post-deploy probe (03:50:21–22, `session_id` null). Until the internal marker
  covers server-side route calls, any nonzero `agent_query` reading is uninterpretable — and the Day-30
  criterion is "≥100 agent queries". Logged as a failure class this sweep; playbook gained an edge-case.
- **P1 — the pSEO footprint grew to 32 indexable routes (20 quarterline + 12 facturgate) on traffic that
  is still 100 % factory-generated.** The 12 new routes came from an owner-approved build scope (no rule-7
  deviation), but the measurement precondition (internal marker + re-based window) is still unimplemented,
  so the same unfalsifiable justification now covers 12 more pages.
- **P1 — the durable-record gap is closed for a second day but still not structural.** Today's entry, the
  gate row and the queue bump are written because this sweep writes them; there is still no producer for
  `context/daily_voice_journal/`. Two consecutive misses happened before; the SOP step is the only thing
  preventing a third.
- **P1 — ParcelProof sits shortlisted with no decision.** FacturGate shipped under an explicit owner
  instruction; the second half of the recon pair (score 80) has no go/no-go, so the build line is idle
  again as of 03:49.
- **P2 — one unexplained measurement anomaly.** A single `factory:page_view` at 09-17 06:03:19 carries
  **no** `session_id` — the only post-instrumentation browser row that does not (the other 3 no-session
  rows since 09-09 are the agent_query probes, which are server-side by design, and the pre-09-09 legacy
  rows). It cannot be counted by any gate and should be classified by the marker work.
- **P2 — the fire-claim fix is still unloaded** (patch in tree since 09-16, gateway process from 09-12).
  Safe to restart only when no cron run is in flight; the daily schedule (08:00 sweep, 10:00 watchdog) and
  the 06:00 pipelines leave windows at ~07:00, ~09:00 and after ~11:00 UTC.

## Founder or fleet notes

- `context/pending_approval.md` remains the authoritative open-work source; this journal is the daily
  audit. Item 3 (manifest) is closed and live-verified; item 4 is half-closed (FacturGate shipped,
  ParcelProof declined-or-proceed pending).
- Unchanged and still true: `/pressflow` 404s on the factory app (the feature lives in
  `editorial-factory` behind auth), all sampled pSEO routes 200, 9 crons active, 0 open incidents, no
  rule-7 deviation.

## Blockers for the sweep to surface

1. **[P0, founder, ~5 min]** `sk_live` in the deploy env **or** record test-mode as intended. Day-30
   (≈09-30) inherits the same $0 otherwise; the 09-16 23:56 checkout click shows demand-side intent the
   test-mode config cannot convert.
2. **[P0, no approval needed, and dated tomorrow]** Echo: work `distribution_queue` (36/36 `ready`,
   5 days untouched). Item #1's deadline is **2026-09-18**; the Q3 wedge already expired 09-15.
3. **[P0, founder decision]** FacturGate's public launch call (`status: beta` today) and **ParcelProof
   go/no-go** — the build line is idle since 03:49.
4. **[P1, @Simon approve, code]** Internal-traffic marker on `events` **including server-side
   `agent_query` rows** + re-based gate window + extend `growth-check.mjs` beyond `quarterline`
   (LedgerLink's 37 rows and every `reconcile_click` stay invisible to the gates).
5. **[P1, Simon / SOP]** Make the sweep's durable record non-optional: a producer for
   `context/daily_voice_journal/` or an explicit "write the entry + gate row + commit" step.
6. **[P1, operator, Hermes fork]** Load the fire-claim fix (`cron/jobs.py` patch is in the tree; the
   2026-09-12 gateway process still runs the old code) — restart `hermes_cli.main gateway run` while no
   cron run is in flight.
7. **[P2, code]** FacturGate GTM surface: `?doc=` shareable scorecard, embeddable badge, and the web-tier
   Stripe products are the deferred P1 items the build report named — none of them move without item 1
   or 2 being answered first.
