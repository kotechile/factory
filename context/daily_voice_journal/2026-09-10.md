# Voice Journal — 2026-09-10

## Shipped / changed
- Approved work landed on `main` and is deployed: `05ac8ae` P0 WebMCP metering + tool-name fix and
  design backlog, `967a1f2` LedgerLink engine + UI, `36ea75c` unique-session instrumentation.
  Tree clean, `main` in sync with `origin/main`. Vitest 33/33 pass.

## Verified today (live, not journal-derived)
- **P0 metering path works in production:** POST `/api/agent/calculate` with
  `x-webmcp-tool: calculate_qbi_deduction` returned 200 and wrote one `agent_query` row with
  `payload.tool = "calculate_qbi_deduction"`. Probe row (`id=206`) deleted afterwards so the gate
  metric stays honest → `agent_query` = 0 real agent queries.
- **No-silent-fallback holds:** `reconcile_stripe_payout` with no customer key returns HTTP 500 +
  explicit error ("No factory key fallback"). Scope guard in `route.ts` reads customer restricted
  key or pasted export only.
- **Browser registration is wired:** `WebMCPProvider` (root layout) → `registerDefaultWebMCPTools()`
  registers all 4 tools on `navigator.modelContext`.
- **Deployed build carries session instrumentation** (`ql_session_id` present in shipped chunk).
- **Build Watchdog recovered:** last run 2026-09-09 10:13 `ok` (was 5× "Interrupted by shutdown");
  next run today 10:00. The fire-claim TTL issue did not recur on the 09-09 run.

## Broke / risks
- **Migration `0002_events_session_id.sql` is NOT applied.** Live probe:
  `column events.session_id does not exist` (42703) → `session_id_column: false`. `telemetry.ts`
  degrades to payload-only, so rows still write (correct), but the indexed column the growth gate
  was designed around is absent. No `psql` and no DB password in `.env`, so this must be applied in
  the Supabase SQL editor. **Growth Watchdog evaluates the Day-7 gate tomorrow (Fri 09-11 17:00).**
- **Day-7 gate is MISSED on honest measurement:** `growth-check.mjs` →
  `unique_sessions: 4` (window 7d, only 4 instrumented rows — all 09-09 deploy smoke traffic) vs the
  ≥50 criterion; export criterion met (2 `export_click`, 9 charges / $161, flat since 09-02).
  Playbook fallback = deploy 20 pSEO routes; `src/lib/seo/presets.ts` holds 11 → **9 short**.
  Gate verdict still unlogged; growth-gate audit trail has no entry after 09-04 until 09-09.
- **`public/.well-known/mcp.json` is stale (P0 not fully closed).** Still advertises
  `calculate_self_employment_2026` (registered nowhere) and lists no LedgerLink tool; unchanged
  since commit `4b3abfd`, no producer script. GTM Vector 4 would publish the dead name.
- **Echo outreach has zero execution record.** P1 approved 09-09, Sept 15 estimated-tax deadline is
  5 days out; the only artifact remains the blueprint (`context/growth_blueprints/2026-08-31_quarterline.md`).

## Founder or fleet notes
- `pending_approval.md` remains the authoritative open-work source; this journal is the daily audit.

## Blockers for the sweep to surface
- Apply migration 0002 (SQL editor) before tomorrow's Growth Watchdog, else the Day-7 verdict stays
  unverifiable for a second cycle.
- Regenerate `.well-known/mcp.json` from `registry.ts` + add the registry/manifest consistency test
  (SOP edge-case recorded in `skills/webmcp_integration.md`).
- Echo: execute GTM Vectors 1 & 5 now (5 days to deadline) — distribution only, no code.
- Day-7 fallback prep: author the 9 missing pSEO presets so the playbook can fire immediately.
