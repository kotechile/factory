# Voice Journal — 2026-09-04

## Shipped / changed
- (none today — no commits since 2026-09-03; see stranded changes under Risks)

## Broke / risks
- Stranded uncommitted work in the tree (from 2026-09-03, never committed/pushed):
  `src/app/api/portal/route.ts` (ESLint prefer-const fix) + `skills/ui_component_standards.md`
  (matching SOP note). Lint fix sitting in working tree = `main` may still fail `verify-build.sh`.
- P0 confirmed: browser WebMCP tools (`calculate_qbi_deduction`, `calculate_quarterly_estimate`)
  compute client-side, bypassing `/api/agent/calculate` — no `track("agent_query")`, no Stripe
  metering. `registry.ts` advertises `calculate_self_employment_2026` (matches no registered tool).

## Founder or fleet notes
- Approval gate is already hard (AGENTS.md rule 7, company_goals.md rule 5, commit 1a60b9f).
  No tightening needed — earlier sweeps re-flagged a closed issue.

## Blockers for the sweep to surface
- P0 (code): route WebMCP handlers through the metered/telemetry path + align registry tool names.
- P1 (outreach, time-sensitive): Echo to execute GTM Vectors 1 & 5 before Sept 15 (11 days out);
  no execution record yet.
- P1 (gate): Day 7 gate due 2026-09-07 — sessions ~20 vs 50 needed; prep 20 pSEO routes now.
- P2 (code): 4 design-backlog wins (CTA price unify, currency mask, tax-liability card emphasis,
  WCAG button contrast) — batch into the next approval.
