# Voice Journal — 2026-09-22

Sweep run 08:00 UTC (Day-22; launch 2026-08-31; Day-14 scored an honest MISS on 09-14; Day-30 due ~09-30,
8 days out — a Wednesday, so the daily sweep scores it and no watchdog run exists that day). Everything below
marked *(live)* was re-probed in this run. Repo changes made this sweep: this entry and refreshed
`STATUS.md` / `pending_approval.md` lines — docs only, no `src/`.

## Shipped / changed since the 09-21 journal

The founder answered the queue overnight and the build line ran twice in one hour. Five commits, two of them
product code:

- **`b45cec2` (2026-09-22 01:33 UTC, Jorge Fernandez, by hand)** — the owner's approval recorded, verbatim:
  *"Pending developments are approved. Tomorrow I will take care of my pending actions (stripe, to-do's list,
  etc)"*. Scoped to the two software items the queue was waiting on the approver for (item 8 and item 9), one
  build slot at a time; his own actions (the live Stripe key, the editorial to-do list) were left alone.
- **`b6e6555` + `a5c8c89` (item 8, QuarterLine retirement finished) — CLOSED, live-verified.** `registry.ts`
  now derives the inventory (`activeProducts` / `retiredProducts` / `defaultInventoryProduct` /
  `isRetiredProduct`) and every generated surface reads it. `/api/checkout` no longer defaults `app` to a
  product literal: an app-less product-scoped plan is an **explicit 400 naming the inventory**, `app=quarterline`
  is a **400 "retired and cannot be purchased"**, `app=ledgerlink` resolves to a sandbox session. The WebMCP
  manifest + agent allowlist derive from the inventory too — **v1.4.0, 6 tools (was 8)**, `tool_selector.default`
  now `reconcile_stripe_payout`; `manifest.test.ts` asserts nothing owned by a retired product is served or
  advertised. Same-class stale literals cleaned (telemetry default product, webhook app fallback, the three
  billing-portal redirects). `a5c8c89` also fixed the **build gate itself**: `verticals:check` compared the
  generated block byte-for-byte *including its own generation date*, so `verify-build.sh` was red on clean
  `main` every calendar day until someone re-ran the sync.
- **`ea56aaf` + `78d0739` + `64af7c1` (item 9, CaseProof) — APPROVED, BUILT, SHIPPED, live-verified.** The
  09-21 recon's single candidate is live at `https://factory.aichieve.net/caseproof` as **`beta`**
  (PRD §5 v1 scope guard only). Deterministic engine `src/lib/calc/caseproof/` (29 new vectors, `npm run test`
  123/123) reproduces the PRD's flip exactly — a proposal claiming **14 months** re-runs at **14.1** and audits
  at **41.0** on the buyer's numbers, with the driver chain reconciling month for month; §179 phase-out at a
  $5,000,000 basis cuts the $2,560,000 limit to $1,650,000; the capex-vs-subscription crossing is at a **40.0%**
  seasonal premium; an unstated maintenance line reports `unstated`, charges $0 and blocks a pass. Three metered
  WebMCP tools at $0.50/call; 6 preset pages; `scripts/verify-build.sh` green end to end (vitest 123 /
  Playwright 41 / visual-QA PASS ×4); deployed image tag == pushed HEAD, published manifest v1.5.0 byte-identical
  to the tree. Two PRD lines are reported with their own honest verdict rather than bent to the prose (the
  crossing *direction*, and attributing +26.6 of the 27-month gap to the vendor's 52-vs-18 FTE claim).

The build line's 3.5-day idle ended: two builds dispatched and shipped inside one hour (01:29 and 01:56 verify
runs). Build slots used: 2. **Item 10 opened** by the same pass — the QuarterLine surfaces the registry
retirement does not cover (the working `/quarterline` calculator whose $9 export now returns an explicit 400,
its 20 indexable preset pages, and `/embed/countdown`). Those are publish/unpublish decisions, queued as an
owner call rather than fixed silently.

## Verified today (live, not journal-derived)

- Prod surfaces *(live)*: `/` 200 · `/caseproof` 200 · `/quarterline` 200 · `/ledgerlink` 200 · `/facturgate`
  200 · `/parcelproof` 200 · **all 44 `/calc/*` presets 200** (caseproof 6 + quarterline 20 + facturgate 12 +
  parcelproof 6; slugs enumerated from the preset files, not sampled).
- Manifest *(live)*: `/.well-known/mcp.json` 200, **v1.5.0, 9 tools**, sha256
  `f3be55bca24d6920c4b583186f375ce9ed9537a3ddd4d9af3ff32f63f75dcb7b` — byte-identical to the working tree.
  CaseProof's three tools are advertised; the retired product's two are gone.
- Paid surface *(live)*: app-less `pdf_audit_export` → **400** with the inventory list; `app=quarterline` →
  **400** retired; `app=ledgerlink` → 200 `cs_test_a10wdt20…`; `/api/portal` → 307 to
  `/?error=no_customer_found`. Item 8 holds on the deployed app, not just in the tree.
- Raw `events` *(live, full read)*: **1099 rows** (parcelproof 345 / facturgate 237 / quarterline 228 / factory
  199 / ledgerlink 90), first 2026-09-01T19:18:36Z, last **2026-09-22T01:59:09Z**; **649 distinct session ids**,
  198 null-session rows. **+176 rows since the 09-21 08:00 sweep**, provenance: 39 = the 09-21 10:00 Build
  Watchdog Playwright run; 136 = the two pre-commit verify runs for the item-8 and item-9 builds (01:29 40 rows,
  01:56–01:59 96 rows); **1 = the returning browser**.
- **`product=caseproof` reads 0 rows** *(live)* — the build deleted its own 90 verification rows (ids 1006–1189)
  after the deploy check, so the growth metric is not reading our tests as customers. Honest and rare; worth
  noting as the pattern to copy.
- `node scripts/growth-check.mjs` *(live)* → `page_view=222` (+4), `checkout_click=4`, `export_click=2`,
  `unique_sessions=119` (+4), `unique_sessions_last_7d=89`, `quarterline_event_rows=228` (+4),
  `session_id_column=true`, `charge_count=9`, `gross_revenue_usd=161`, `days_since_first_event=21`.
- **`agent_query` is still 4** — facturgate ×3 (09-17 03:50) + parcelproof ×1 (09-18 22:07), every one a factory
  ship probe. **Organic agent queries: 0 on day 21.** Day-30's agent criterion is ≥100.
- **Production is still Stripe practice mode** *(live, Stripe API read-back)*: fresh `app=ledgerlink` session
  `cs_test_a10wdt20…`, `livemode:false`; all **4** `purchases` rows `cs_test_*` ($9.00, all 2026-09-02); the
  single `subscriptions` row `canceled`. **Real collected revenue = $0.**
- **The returning browser came back again** *(live)*: session `30eb9935…`, id 964, **2026-09-22T01:25:12Z**
  (`factory`) — **24 rows lifetime**, four products, now seen on **seven separate days** (09-10, 09-13, 09-14,
  09-16, 09-20, 09-21, 09-22). Still no UA/referrer/path stored → **0 provably-external sessions in 22 days**,
  and it cannot be separated from the owner's own browser without tagging at source (item 5).
- `factory_config.distribution_queue` *(live)*: still **`[]`**, `updated_at` 2026-09-19T22:42:49Z (third day).
- CaseProof build job `38704fd5a527` *(live)*: fired 01:36:29, **completed 02:06:48, `ok`**, execution
  `40042aae…`; it posted its own Slack report ~02:06 asking for the launch call — this sweep repeats that ask
  rather than assuming it was read.
- Crons *(live)*: 12 jobs (9 active + 3 completed one-shot builds), **0 open incidents** (all ledger rows
  `closed`). **The 09-22 06:00 `enterprise_tech_leadership` run recorded `ok`** — it logged `completed
  successfully` at 06:05:28 and no fence timeout followed, so the fire-claim mislabel predicted for today did
  **not** recur. That leaves only the 09-16 `gpu_hardware` label (next run 09-23) as a visible symptom.
- `node scripts/vertical-sync.mjs --check` *(live)* → exit 0, "26 verticals, no drift".
- Repo *(live)*: `HEAD == origin/main == 64af7c1`, tree clean except the pre-existing untracked `.hermes/`
  scratch dir.

## Broke / risks

- **P0 — $0 real revenue, 8 days to the Day-30 gate.** Production has served practice checkout pages for 22
  days (`livemode:false`, measured from the API). Day-30 (≈09-30) inherits $0 unless the credential lands or
  practice mode is recorded as intended. The owner said on 09-22 he would handle Stripe today — unverified so
  far.
- **P0 — three finished products are invisible to the public** (all `beta`, launch calls with the founder):
  FacturGate since 09-17 (5 days), ParcelProof since 09-18 (4 days), CaseProof since 09-22. The directory is the
  only surface a stranger can reach, and nothing can reach a product until it is launched.
- **P0 — the retirement still leaves three QuarterLine surfaces public (item 10, new).** The `/quarterline`
  calculator works but its $9 export button returns an explicit error — honest, and a dead end for a visitor;
  20 preset pages remain served and indexable; `/embed/countdown` still links there.
- **P1 — the agent leg of the Day-30 gate is 4/4 factory smoke** vs the ≥100 criterion; organic agent demand is
  0 on day 21. The retirement fix reduced the agent-visible surface (the manifest lost the two retired tools),
  which is correct and also means the number cannot move without a launch or the marker.
- **P1 — the internal-traffic marker is still unbuilt (item 5)** and now needs a production Supabase migration
  only the owner can apply. Today's evidence keeps it sharp: a seventh visit from the same unattributable
  browser; every gate still reads numbers the factory generates.
- **P1 — one open correctness question on the shipped ParcelProof** (UPS AHS-Dimension trigger 96″ as published
  vs 48″ as intended; one line in `src/lib/calc/parcelaudit/surcharges.ts`) — asked 09-18, still unanswered
  (5th day).
- **P1 — the durable-record step held for a seventh day, still non-structural** (item 6). Today's entry exists
  because this sweep writes it; `context/daily_voice_journal/` still has no producer.
- **P2 — the fire-claim fix is still unloaded** (`cron/jobs.py` mtime 2026-09-16, gateway pid `1963330` from
  2026-09-12 15:13), but its last visible symptom is now a single stale label. The mislabel did not recur on
  09-22 with a two-build hour and the 06:00 pipeline run; restart before 06:00 on 09-23 removes the last one.
  Never restart while a cron run is in flight.

## Founder or fleet notes

- `context/pending_approval.md` remains the authoritative open-work source; this journal is the daily audit.
  Items 2, 3, 4, 8 and 9 are closed; **item 10 is new**; items 1, 5, 6, 7 remain open.
- The build line is no longer idle-starved: two builds shipped today, and the next queue lead is item 10's
  publish/unpublish decision (owner) ahead of item 5 (needs the owner's migration).
- Unchanged and still true: `/pressflow` 404s here (that feature lives behind auth in `editorial-factory`), no
  rule-7 deviation, and every number in this entry is a live probe or a named commit.

## Blockers for the sweep to surface

1. **[P0, founder, one reply]** Launch calls for **FacturGate, ParcelProof and CaseProof** (all `beta`) —
   `@Simon approve launch FacturGate, ParcelProof, CaseProof`. Keep going: all three stay unpromoted.
2. **[P0, founder, ~5 min]** Live payment key in the deploy env **or** record practice mode as intended —
   8 days to Day-30, which otherwise inherits the same $0.
3. **[P0, owner decision]** Item 10 — the three public QuarterLine surfaces (calculator + 20 preset pages +
   embed). `@Simon approve retire QuarterLine pages` to have the calculator replaced with the retirement notice.
4. **[P1, founder, one line]** Confirm the UPS AHS-Dimension trigger (96″ as published vs 48″ as intended) —
   5th day open.
5. **[P1, @Simon approve + owner migration]** Internal-traffic marker on `events` covering server-side
   `agent_query` rows + a re-based gate window; the Growth Watchdog formally gates Day-30 on it.
6. **[P1, Simon / SOP]** Make the sweep's durable record non-optional: a producer for
   `context/daily_voice_journal/` or an explicit "write the entry + gate row + commit" step.
7. **[P2, operator, Hermes fork]** Load the fire-claim fix — restart `hermes_cli.main gateway run` while no
   cron run is in flight, ideally before 06:00 on 09-23 (the last stale label's next run).
