# Voice Journal — 2026-09-12

Sweep run 08:00 UTC. Everything below is a live probe from this run unless it names a commit.

## Shipped / changed (since the 09-11 08:00 sweep)
- `dd1251a` (09-11 17:06) **feat(growth): pSEO presets 9 → 20** — the Day-7 fallback was **fired** by the
  Growth Watchdog. `src/lib/seo/presets.ts` now holds **20** presets; verified live this sweep — every
  new slug returns 200 (`florida-llc-estimated-tax-2026`, `washington-1099-contractor-tax-2026`,
  `georgia-freelancer-quarterly-tax-2026`, `realtor-qbi-deduction-2026`, `author-royalties-schedule-c-2026`,
  `photographer-schedule-c-tax-2026`, `consultant-mfj-qbi-phaseout-2026`, `100k-self-employment-tax-2026`,
  `300k-schedule-c-taxes-2026`, `500k-qbi-phaseout-2026`, `side-hustle-gig-tax-2026`).
- `cf26fec` (09-11 17:06) Growth Watchdog logged the Day-7 confirm + a **Stripe test-mode** finding.
- **Migration `0002_events_session_id.sql` is APPLIED** (09-11 P0 item 3 — CLOSED). Independent probe this
  sweep: `select session_id from events limit 1` → HTTP 200 (was `42703`). `growth-check.mjs` reports
  `session_id_column: true`.
- `main` in sync with `origin/main` at `cf26fec`; nothing committed since.

## Verified today (live, not journal-derived)
- `node scripts/growth-check.mjs` (08:0x UTC) → `page_view=115`, `unique_sessions=15`,
  `unique_sessions_last_7d=15`, `session_instrumented_rows=15`, `session_id_column=true`,
  `quarterline_event_rows=120`, `checkout_click=3`, `export_click=2`, `charge_count=9`,
  `gross_revenue_usd=161`, `days_since_first_event=11`.
- **All 15 unique sessions are still factory-generated** (raw `events` provenance read; 231 rows total,
  last write 09-11 17:05:45): 4 = 09-09 20:52 deploy smoke, 4 = 09-10 10:01 Build Watchdog Playwright,
  4 = 09-11 10:01 Build Watchdog Playwright, 3 = 09-11 17:05 Growth Watchdog Playwright. External
  sessions instrumented to date: **0**.
- **`agent_query` = 0 rows, all-time** (exact-count probe). The `$0.25/query` agent tier has still never
  been exercised by a real caller.
- **Revenue is test-mode.** `STRIPE_SECRET_KEY=sk_test` in `/root/software-factory-core/.env` → the
  `$161` / 9 charges are Stripe **test-mode** charges. Real collected revenue = $0.
- **`.well-known/mcp.json` still stale.** Prod (`https://factory.aichieve.net/.well-known/mcp.json`, 200)
  is byte-identical to the working tree and still advertises only `calculate_self_employment_2026`
  (registered nowhere), while `registry.ts` registers `calculate_qbi_deduction`,
  `calculate_quarterly_estimate`, `format_article_for_linkedin`, `reconcile_stripe_payout`. Dead name
  still live; now the pSEO routes are pushing traffic at it.
- Build Watchdog last run 09-11 10:00 `completed`; Growth Watchdog ran 09-11 17:00 `completed`.

## Broke / risks
- **The working tree is dirty with an unfinished feature, and it fails the build gate.** 7 modified files
  (2,267 insertions / 596 deletions) + new `src/app/api/distribution/tasks/route.ts` +
  `supabase/migrations/0003_distribution_tasks.sql`; mtimes 02:20–02:23 UTC today, written while an
  Antigravity IDE server has this workspace open (started 02:11, still running). It is a PressFlow
  "Weekly Multi-Platform Distribution & To-Do Queue". Measured this sweep:
  - `npm run lint` → **exit 1**: 1 error `src/app/pressflow/page.tsx:316` (`react-hooks/set-state-in-effect`)
    + 5 warnings. `verify-build.sh` runs under `set -e` → **the 10:00 Build Watchdog fails at step 2**.
  - `npx tsc --noEmit` → clean; `check:tokens` → clean; content-distributor vitest → 8/8 pass.
  - The rewritten `page.tsx` (1515 → 2359 lines) drops copy asserted by `tests/e2e/pressflow.spec.ts`:
    "Editorial Factory Suite", "Load Sample", "LinkedIn Format Generator & Publisher" are gone (the `h1`
    "PressFlow" survives) → e2e test 1 fails even after the lint error is fixed.
  - `public.distribution_tasks` **does not exist in production** (PostgREST `PGRST205`) → the new
    `/api/distribution/tasks` route cannot serve; the UI's to-do list cannot load.
  - No approval record for this feature anywhere in `context/` (AGENTS.md rule 7).
- **The Day-7 fallback fired ahead of its own preconditions.** The 09-11 08:00 sweep logged "do not fire
  the fallback until (a) internal sessions are tagged/excluded and (b) the window is re-based to
  instrumentation start"; the Growth Watchdog shipped 20 pSEO routes 9 hours later, at 09-11 17:06. The
  routes are live and indexable, verified by a metric that is 100% our own traffic. Neither precondition
  has been implemented (no internal marker exists in `events`).
- **Day-14 revenue gate is due ~09-14 (Monday) and nothing will evaluate it.** The Growth Watchdog runs
  Fridays only (next 09-18, 4 days late), and with `sk_test` it is unmeetable regardless.
- **Echo outreach: still zero execution record, 3 days to the Sept 15 deadline.** Only artifact remains
  `context/growth_blueprints/2026-08-31_quarterline.md`. Ironically the in-flight (broken, unapproved)
  distribution queue above is exactly the tooling that would execute it.
- Journal still has no automated producer (`README.md` says manual) — a skipped sweep is a silent audit gap.

## Founder or fleet notes
- `context/pending_approval.md` remains the authoritative open-work source; today's live state, the 0002
  closure, the pSEO delivery and the dirty-tree/lint blocker are written back into it this sweep.

## Blockers for the sweep to surface
- **[P0, decision needed before 10:00 UTC]** Dirty tree + `lint exit 1` → Build Watchdog will report a red
  gate that is not a regression from `main`. Decide: stash/revert the in-flight work, or finish it (fix the
  `set-state-in-effect` error, restore or update the pressflow e2e copy, apply migration 0003).
- **[P0, founder]** Set a live `sk_live` Stripe key in the deploy env, or accept and record that the
  revenue gate is being scored in test mode.
- **[P0, approval needed]** Generate `.well-known/mcp.json` from `src/products/registry.ts` + consistency
  test — unchanged since 09-10.
- **[P1, approval needed]** Tag internal/a QA traffic at source and re-base the gate window, so the
  already-fired fallback can be judged honestly.
- **[P1, no approval needed]** Echo: GTM Vectors 1 & 5 — distribution only, 3 days left.
