# Voice Journal — 2026-09-23

Sweep run 08:00 UTC (Day-23; launch 2026-08-31; Day-14 scored an honest MISS on 09-14; Day-30 due ~09-30,
**7 days out** — a Wednesday, so the daily sweep scores it and no watchdog run exists that day). Everything
below marked *(live)* was re-probed in this run. Repo changes made this sweep: this entry and refreshed
`STATUS.md` / `pending_approval.md` lines — docs only, no `src/`.

## Shipped / changed since the 09-22 journal

Three commits landed, all by the owner, by hand, none from a scheduled factory run:

- **`dfc2851` (2026-09-22 23:56 UTC)** — `feat(stripe): resolve sandbox/live from STRIPE_MODE, never from whichever
  key is present`. A new single resolver (`src/lib/stripe/mode.ts`, 201 lines + 21 mode-contract vectors): the mode
  is an explicit `STRIPE_MODE=test|live` plus mode-paired keys/secrets; a key whose prefix disagrees with the mode
  **throws**, and a missing `STRIPE_MODE` is an explicit failure rather than an inference. `/api/checkout`'s dev
  fallback moved behind `NODE_ENV !== "production"` so a production deploy with no usable config returns 500 instead
  of handing a buyer a simulated session; the webhook no longer echoes the first 8 characters of the secret to an
  unauthenticated caller; `growth-check.mjs` now reports `stripe_mode` and flags sandbox charges as
  `revenue_is_sandbox`. This is the direct fix for the 09-11 finding ($161 counted as revenue while sandbox).
- **`9eb43a5` (2026-09-23 01:57 UTC)** — `feat(domain): migrate to apps.giniloh.com and relocate showcase to
  /showcase`. Root `/` now 307s to `https://giniloh.com`; the directory moved to `/showcase`; `NEXT_PUBLIC_APP_URL`,
  the manifest URL, the checkout/portal/webhook/embed URLs and `AGENTS.md` were updated in the same commit.
  **Legacy `factory.aichieve.net` is now 503 on every path** — verified `/`, `/showcase`, `/caseproof` *(live)*.
- **`1393f42` (2026-09-23 02:57 UTC)** — `chore(verticals): re-vendor the editorial registry after the cron
  coverage fix` (editorial-factory `ed90c8b` gave all 26 verticals unique 30-min slots and made its cron sync
  reconcile instead of create-only). Data + ledger only; `vertical-sync.mjs --check` exit 0 *(live)*.

## Verified today (live, not journal-derived)

- **Deployed image == pushed HEAD**: `af8yqbwrrnyyfgs9wcg0intj:1393f42847feeae276eb2dd3ab5cb9be2e4f7d70` on the
  running container. All three commits are live.
- Prod surfaces on the **new** host *(live)*: `/` **307** → `https://giniloh.com/` · `/showcase` 200 ·
  `/ledgerlink` `/facturgate` `/parcelproof` `/caseproof` `/quarterline` `/embed/countdown` all 200 · **all 44
  `/calc/*` presets 200** (slugs enumerated from the four preset files: caseproof 6 + quarterline 20 + facturgate
  12 + parcelproof 6). No `factory.aichieve.net` reference remains in the served HTML of `/showcase`, `/caseproof`,
  `/ledgerlink`; `/embed/countdown` now links `apps.giniloh.com/quarterline`.
- Manifest *(live)*: `/.well-known/mcp.json` 200, **v1.5.0, 9 tools**, sha256
  `2843f352f28fbf1adae82c9fd6ad3808932f99e23043e210c4aa0853acf232ea` — byte-identical to the working tree;
  `endpoint` is the new host.
- Paid surface *(live)*: `app=ledgerlink` → **200** `cs_test_a1l9pF2VmGwT3oDxyoyh5dvCJ199BQZYGbjPH4ccQGlqX4l3mv6hGtO0p7`
  (sandbox); app-less `pdf_audit_export` → **400** naming the inventory; `app=quarterline` → **400** retired;
  `/api/portal` → 307 `?error=no_customer_found`. Item 8 still holds on the new host.
- **P0 — the live payment key slot holds a key ID, not a key.** The deploy env now carries the full mode split
  (`STRIPE_MODE=test`, `STRIPE_SECRET_KEY_TEST=sk_test_…`, `STRIPE_WEBHOOK_SECRET_TEST=…`) **and** a live pair whose
  secret is `mk_1UAeGxJaTDc3aAp0lG3UwbFj`. Read-only `GET https://api.stripe.com/v1/balance` with that value →
  **HTTP 401**, verbatim: *"Invalid API key provided: mk_1UAeG***************wbFj. This looks like the ID of an API
  key rather than the key itself. API keys typically start with `pk_`, `rk_`, or `sk_`."* The owner has therefore
  acted on item 1 (the pair exists, `STRIPE_MODE` is set) but pasted the key's **ID**. `mode.ts` requires the
  `sk_live_` prefix in live mode, so flipping `STRIPE_MODE=live` as-is is an explicit throw (rule 5: honest failure,
  not a silent wrong-mode charge) — and it is the one write that stands between $0 and real revenue.
- Raw `events` *(live, full read)*: **1419 rows** (parcelproof 440 / facturgate 297 / quarterline 228 / factory 219
  / ledgerlink 121 / caseproof 114), first 2026-09-01T19:18:36Z, last **2026-09-23T02:42:28Z**; **858 distinct
  session ids**, 198 null-session rows. **+320 since the 09-22 08:00 sweep**, provenance: 124 = the 09-22 10:02/10:04
  Build Watchdog Playwright run; 189 = the three pre-commit verify runs behind `dfc2851` (09-22 23:55) and
  `9eb43a5` (09-23 01:54 + 01:56); **7 = five new standalone sessions** (below).
- **Five new unattributable sessions in 26 hours** *(live)* — against six in the previous 21 days:
  `3eb270ce` 09-22 21:48:52Z (factory) · `ac8303ae` 09-23 01:38:00Z (factory) · `59a27597` 01:39:39 factory →
  01:39:50 ledgerlink · `24c8fd67` 01:40:10 (factory) · `a3f8de07` 02:41:57 factory → 02:42:28 ledgerlink. Two of
  them show the directory-then-product pattern (factory page, a product page ~30 s later) that no factory test run
  produces. They straddle the 01:57 domain migration, so the likeliest reading is the owner checking the site
  before and after the move — but with no user-agent or referrer stored they remain **unattributable, not
  provably external** (item 5). The long-running returning browser `30eb9935…` (24 rows, seven visit days
  09-10 → 09-22) **did not return on 09-23**.
- **`product=caseproof` now reads 114 rows** *(live)* — 100% factory verification traffic from the 09-22 10:0x
  Build Watchdog and the 09-23 01:5x verify runs. The 09-22 build deleted its own 90 rows after the deploy check;
  that discipline was not repeated by the later runs, so the caseproof number is again our own tests.
- **`agent_query` is still 4** — facturgate ×3 (09-17 03:50) + parcelproof ×1 (09-18 22:07), every one a factory
  ship probe. **Organic agent queries: 0 on day 23.** Day-30's agent criterion is ≥100.
- **Production is still Stripe sandbox** *(live, API read-back)*: 4 `purchases` rows all `cs_test_*` ($9.00, all
  2026-09-02); the only `subscriptions` row `canceled`. **Real collected revenue = $0.** `growth-check.mjs` now
  says so itself: `stripe_mode: "test"`, `revenue_is_sandbox: true`, `gross_revenue_usd 161`,
  `charge_count 9`, `page_view 222`, `unique_sessions 119`, `unique_sessions_last_7d 86`.
- `factory_config.distribution_queue` *(live)*: still **`[]`**, `updated_at` 2026-09-19T22:42:49Z (fourth day).
- Crons *(live)*: 30 jobs now (the editorial registry's 26-vertical menu maps to live pipeline jobs), **0 open
  incidents**. **`gpu_hardware` ran 2026-09-23 07:13:13Z `ok`** — the last stale `Interrupted by shutdown` label is
  cleared, so the fire-claim bug now has **no visible symptom**; the running gateway is still the 2026-09-12
  process, so the fix still needs a restart to load (item 7, operator action, no longer urgent).
- `node scripts/vertical-sync.mjs --check` *(live)* → exit 0, "26 verticals, no drift".
- **Domain-migration leftovers** *(live)*: `public/.well-known/ai-plugin.json` is served 200 on the new host but its
  `api.url` and `legal_info_url` still name the dead `factory.aichieve.net`, and its `/openapi.json` companion
  404s; `/robots.txt` and `/sitemap.xml` 404 with **no producer anywhere in the repo** (44 indexable landing pages
  and nothing maps them for crawlers). Both are publish/code decisions, not silent fixes.
- Repo *(live)*: `HEAD == origin/main == 1393f42`, tree clean except the pre-existing untracked `.hermes/`
  scratch dir.

## Broke / risks

- **P0 — the live key slot is filled with the wrong value, 7 days to the Day-30 gate.** Measured, not inferred
  (Stripe returns 401 for it). Everything else about the mode split is correct, so the fix is one paste of the
  `sk_live_…` value. Day-30 (≈09-30) inherits $0 until then.
- **P0 — three finished products are invisible to the public** (all `beta`): FacturGate since 09-17 (6 days),
  ParcelProof since 09-18 (5 days), CaseProof since 09-22 (1 day). The directory is the only surface a stranger can
  reach and nothing is promoted from it.
- **P0 — the retirement still leaves three QuarterLine surfaces public (item 10).** Unchanged and re-verified:
  `/quarterline` renders the working calculator whose $9 export returns an explicit 400 (dead end for a visitor),
  its 20 preset pages are still served and indexable, `/embed/countdown` still links there.
- **P1 — the Day-30 agent leg is 4/4 factory smoke** vs the ≥100 criterion; organic agent demand 0 on day 23. The
  retirement fix correctly shrank the agent-visible surface, so the number cannot move without a launch or the marker.
- **P1 — the internal-traffic marker is still unbuilt (item 5)**, and today's evidence sharpens it: five new
  visitor-like sessions in a day cannot be separated from the owner's own browser without tagging at source, and
  every growth gate still reads numbers we generate.
- **P1 — one open correctness question on the shipped ParcelProof** (UPS AHS-Dimension trigger 96″ as published vs
  48″ as intended; one line in `src/lib/calc/parcelaudit/surcharges.ts`) — asked 09-18, still unanswered (6th day).
- **P1 — the durable-record step held for an eighth day, still non-structural** (item 6): today's entry exists
  because this sweep writes it. `context/daily_voice_journal/` still has no producer.
- **P2 — the domain migration left two public leftovers** (above): a descriptor file advertising a dead host, and
  no crawler map for 44 landing pages. Small, but exactly the class of stale literal item 8 was opened for.
- **P2 — item 7 downgraded:** the fire-claim patch is still unloaded, but the last stale label cleared itself this
  morning. Restart when convenient; never while a cron run is in flight (10:00 Build Watchdog daily, 17:00 Fri Growth).

## Founder or fleet notes

- `context/pending_approval.md` remains the authoritative open-work source; this journal is the daily audit.
  Items 2, 3, 4, 8 and 9 are closed; **item 10 open**; items 1, 5, 6, 7 remain open and a new **item 11** records
  the migration leftovers.
- The build line has been idle since the two 09-22 builds (≈30 h). The queue leads are all owner decisions now:
  item 1's key (one paste), the three launch calls, item 10's publish decision, then item 5's migration.
- The owner is clearly working the repo himself — three commits in 24 h across payments and hosting. The sweep's
  job is to measure what those commits did, which is what this entry does; nothing here was built or shipped
  outside the gate.
- Unchanged and still true: `/pressflow` 404s here, no rule-7 deviation, and every number in this entry is a live
  probe or a named commit.

## Blockers for the sweep to surface

1. **[P0, founder, one paste]** The live key slot holds the key's **ID** (`mk_…`) — Stripe rejects it with 401.
   Paste the real `sk_live_…` value and set `STRIPE_MODE=live`, or record practice mode as intended.
2. **[P0, founder, one reply]** Launch calls for **FacturGate, ParcelProof and CaseProof** —
   `@Simon approve launch FacturGate, ParcelProof, CaseProof`.
3. **[P0, owner decision]** Item 10 — the three public QuarterLine surfaces.
   `@Simon approve retire QuarterLine pages`.
4. **[P1, @Simon approve + owner migration]** Internal-traffic marker on `events` + a re-based gate window
   (item 5); the Growth Watchdog formally gates Day-30 on it.
5. **[P1, founder, one line]** Confirm the UPS AHS-Dimension trigger (96″ as published vs 48″ as intended) —
   6th day open.
6. **[P1, Simon / SOP]** Make the sweep's durable record non-optional (item 6).
7. **[P2, @Simon approve, code]** The migration leftovers: repoint `ai-plugin.json` off the dead host and add a
   crawler map (`robots.txt` / `sitemap.xml`) for the 44 landing pages.
8. **[P2, operator]** Restart the Hermes gateway when no cron run is in flight to load the fire-claim fix
   (no visible symptom left).
