# Voice Journal — 2026-09-26

Sweep run 08:00 UTC (Day-26; launch 2026-08-31; Day-14 scored an honest MISS on 09-14; Day-30 due
**2026-09-30 — 4 days out**, a Wednesday, so the daily sweep scores it and no watchdog run exists that day; the
last Growth Watchdog ran **09-25 17:00** `ok` and logged its gate row). Everything below marked *(live)* was
re-probed in this run. Repo changes made this sweep: this entry and refreshed `STATUS.md` /
`pending_approval.md` header lines — docs only, no `src/`.

## Shipped / changed since the 09-25 journal

**Nothing was built or shipped in this repo.** `HEAD == origin/main == 50014d4` (the 09-25 Growth Watchdog's own
docs commit) and the running app image is `af8yqbwrrnyyfgs9wcg0intj:50014d46fc81…` *(live, `docker ps`, up
15 h)*, i.e. that same docs commit. No commit in the factory repo in 24 h; the build line has been idle since
the two 09-22 builds (≈94 h).

Five measurable changes, none from a factory build:

- **P0 RESOLVED — the four articles that vanished 09-25 00:36 are back, and the editorial engine had its
  busiest night in a week.** The pressflow container was rebuilt this morning (`nstjdswcf5p9xckwja89o6z0`
  image `536219b…` == the editorial repo's HEAD, up "About an hour" at 08:00) and `/app/published` now holds
  **15 files, dir mtime 06:39** *(live, `docker exec ls -la`)*; `pressflow /api/articles.json` returns
  **2 → 15** *(live)*. All four deleted slugs are back (`nvidia-hugging-face-moat`,
  `rollback-breaks-execution-continuity`, `temporal-durable-execution-12-55b`,
  `unfi-warehouse-automation-capex`) plus the two other articles that were waiting, plus three new ones
  published today (supply_chain `tariff-cliff-already-priced-in`, `reshoring-moved-the-tariff-upstream`,
  personal_microeconomics `disney-hulu-fourth-hike-subscription-creep`). The prediction in yesterday's entry
  was exact: the repo still carried all of them, so the rebuild redeployed them. The deletion itself stays
  **unattributable** (traefik has no access logs; pressflow logs only its startup banner) but is no longer
  live-impacting.
- **The published record and the live site still disagree by five articles.** Supabase `articles` went
  2 → **10 rows** *(live, service-role read)* while the site serves **15**: `nvidia-hugging-face-moat`,
  `rollback-breaks-execution-continuity`, `temporal-durable-execution-12-55b`,
  `unfi-warehouse-automation-capex` and `ai-price-volatility-build-vs-buy-maintenance-tax` are live as files
  and listed in `context/published_log.md` but have no Supabase row. New observation, P2 (nothing user-facing
  reads those rows today).
- **The social queue is now produced by the pipeline, not by hand: 6 → 38 items.** `factory_config.distribution_queue`
  `updated_at` **2026-09-26T06:41:31Z**, 38 cards, **all `ready`, 0 published** *(live)*, covering 15 articles ×
  Reddit + LinkedIn. The producer is the editorial repo's own change (`scripts/seed_distribution.py`, called as
  step 4c of the publish pass — commit `5c7df8a`, "prep the to-do cards automatically in the publish pass",
  authored in response to the founder's "to-do cards manual is great" note). The 09-24/09-25 notepad line
  "no factory cron produces this queue" is now **superseded**: a cron does. Nothing has ever been published
  from it, and the oldest cards now describe 09-21…09-23 news.
- **Traffic: 62 new rows, all of them our own test run, and none since.** `events` **1547 → 1609 rows**
  *(live, full paged read)*; last event **2026-09-25T10:02:14Z** → **≈22 h with no event at all**. Every 09-24
  and 09-25 row falls inside the 10:00–10:05 window: the daily Build Watchdog Playwright run (`caseproof
  / facturgate / parcelproof / ledgerlink / factory`), 62 rows in 13 seconds. Distinct sessions 940 → **981**
  (every new id is that run). **The last non-test event is still 2026-09-23T14:11:42Z** (session `59a27597…`
  on the factory page) — the site has had no visitor of any kind for 3 days.
- **`product=caseproof` 159 → 181 rows** *(live)*, all of it the same 10:02 test run. Sixth consecutive day
  this number is our own verification traffic. `quarterline` 7-day unique sessions **65 → 32** *(live,
  `growth-check.mjs`)* — the window rolled past the 09-19/09-20 cluster and nothing replaced it; quarterline
  has had no event since 09-21.

## Verified today (live, not journal-derived)

- Prod surfaces on `apps.giniloh.com` *(live)*: `/` **307** → `https://giniloh.com` (`giniloh.com` itself
  **200**) · `/showcase`, `/ledgerlink`, `/facturgate`, `/parcelproof`, `/caseproof`, `/quarterline`,
  `/embed/countdown` all **200** · legacy `factory.aichieve.net` still **503**. No regression.
- **All 20 `/quarterline/calc/*` presets 200** *(live, every slug read out of `src/lib/seo/presets.ts` and
  probed one by one)*. **Item 10 therefore unchanged**: the retired product's calculator page, its 20
  indexable landing pages and `/embed/countdown` are all still public.
- Manifest *(live)*: `/.well-known/mcp.json` **200, v1.5.0, 9 tools**, sha256
  `2843f352f28fbf1adae82c9fd6ad3808932f99e23043e210c4aa0853acf232ea` — **byte-identical** to the tree.
- Paid surface *(live, POST)*: `{plan:pdf_audit_export, app:ledgerlink}` → **200** `cs_test_a1Xz1Nt6…` ·
  `app=caseproof` → **200** `cs_test_a1PCnHy3…` · app-less → **400** naming the inventory · `app=quarterline`
  → **400** "retired and cannot be purchased". `/api/portal` → **307** `?error=no_customer_found`.
  **Item 8 holds.**
- **P0 — the live payment key slot still holds a key ID, and Stripe still rejects it.** Read-only
  `GET https://api.stripe.com/v1/balance` run *inside the running app container* with the deployed value →
  **HTTP 401**, verbatim: *"Invalid API key provided: mk_1UAeG***************wbFj. This looks like the ID of an
  API key rather than the key itself."* The test key in the same env returns **200** (the sandbox works).
  Deploy env unchanged: `STRIPE_MODE=test`, `STRIPE_SECRET_KEY_TEST=sk_test_…`,
  `STRIPE_WEBHOOK_SECRET_TEST=whsec_g6Jc…`, `STRIPE_SECRET_KEY_LIVE=mk_1UAeGxJaTDc3aAp0lG3UwbFj`,
  `STRIPE_WEBHOOK_SECRET_LIVE=whsec_vn7B…`. Day-30 inherits $0 until the real `sk_live_…` value is pasted.
- Raw `events` *(live, full paged read)*: **1609 rows** (parcelproof 497 / facturgate 333 / factory 231 /
  quarterline 228 / caseproof 181 / ledgerlink 139), **981 distinct session ids**, 198 null-session rows,
  4 `agent_query`.
- **`agent_query` is still 4** — facturgate ×3 (09-17 03:50) + parcelproof ×1 (09-18 22:07), every one a
  factory ship probe. **Organic agent queries: 0 on day 26.** Day-30's criterion is ≥100.
- **No real revenue** *(live, API read-back + DB)*: 4 `purchases` rows all `cs_test_*` ($9.00, all
  2026-09-02); the single `subscriptions` row `canceled`. **$0 collected, 26 days in.**
- Editorial fleet *(live)*: pressflow `/healthz` **200**; factory crons **31 active, `hermes cron doctor`
  clean** (no issues, no stale labels), 22 with a last run — **all `ok`**, 9 registered verticals never run.
  New today: **"Editorial Verify Gate"** (`30 9 * * *`, created 2026-09-26T01:34, ran `ok` at 01:34) — a daily
  `verify.sh` + pressflow-image-vs-HEAD check that is silent when green. Today's editorial publishes:
  `personal_microeconomics…` 06:11, `supplier_risk_reshoring_decision` 06:42, both `ok`; the 09-25 Growth
  Watchdog ran 17:06 `ok` and logged its gate row (commit `50014d4`). Next Growth Watchdog: **10-02 17:00**.
- `node scripts/vertical-sync.mjs --check` *(live)* → exit 0, "26 verticals, no drift (vendored)".
- **Domain-migration leftovers unchanged** *(live)*: `/.well-known/ai-plugin.json` 200 naming the dead host;
  `/openapi.json`, `/robots.txt` and `/sitemap.xml` all **404** with no producer, for 44 indexable landing
  pages.
- Repo *(live)*: `HEAD == origin/main == 50014d4`, tree clean except the pre-existing untracked `.hermes/`.

## Broke / risks

- **P0 — the live key slot is still wrong, now 4 days from Day-30.** Re-measured, not inferred. One paste of
  the real `sk_live_…` (+ `STRIPE_MODE=live`) is the whole fix.
- **P0 — three finished products are still invisible to the public** (all `beta`): FacturGate since 09-17
  (9 days), ParcelProof since 09-18 (8 days), CaseProof since 09-22 (4 days). No commit touched their status.
- **P0 — the retirement still leaves three QuarterLine surfaces public (item 10).** Re-verified today end to
  end: `/quarterline` 200 with a working calculator whose $9 export returns an explicit 400, all 20 preset
  pages 200, `/embed/countdown` 200.
- **P1 — the Day-30 agent leg is 4/4 factory smoke** vs the ≥100 criterion; organic agent demand 0, sixth day
  unchanged. Day-30 (09-30) will read zero on all three legs unless the founder's actions land first.
- **P1 — the internal-traffic marker (item 5) is still unbuilt.** Today adds the same evidence as yesterday:
  62 of 62 new rows were ours, so every growth gate reads numbers we generate.
- **P1 — the ParcelProof correctness question is now 9 days open** (UPS AHS-Dimension trigger 96″ as published
  vs 48″ as intended; one line in `src/lib/calc/parcelaudit/surcharges.ts`).
- **P1 — the durable-record step held for an eleventh day, still non-structural** (item 6): today's entry
  exists because this sweep writes it. `context/daily_voice_journal/` still has no producer.
- **P2 NEW — five articles are live but absent from Supabase `articles`** (10 rows vs 15 files); the live site
  and the database disagree.
- **P2 — item 11 leftovers** (dead-host descriptor + no crawler map) unchanged; **item 7** (gateway restart, no
  visible symptom) unchanged; cron doctor is clean, so nothing is mislabelled.
- **Observation, continuing:** the written-and-waiting social queue is now 38 cards (up from 6), none
  published, and the oldest describe 09-21…09-23 news that is already aging out.

## Founder or fleet notes

- `context/pending_approval.md` remains the authoritative open-work source; this journal is the daily audit.
  Items 2, 3, 4, 8 and 9 are closed; **open: 1 (live key), 5 (traffic marker), 6 (durable record), 7 (gateway
  restart, P2), 10 (QuarterLine surfaces), 11 (migration leftovers)** — plus the three launch calls, the
  ParcelProof one-liner, and the now-lower-priority article-deletion question (the rebuild restored the
  articles, so it is a guard-rail question, not a live one).
- Every queue lead is still an owner decision: one paste, three launch approvals, one retire approval, one
  data confirmation, and one distribution approval. The fleet has nothing buildable ahead of those.
- The editorial engine, by contrast, shipped ~15 commits overnight by itself (article restore + Supabase sync,
  autonomous multi-topic synthesis, automatic to-do cards, an observer-voice change, the new daily verify
  gate) — all in `kotechile/Editorial-Factory`, none in this repo.
- Unchanged and still true: `/pressflow` 404s on the factory host (it lives at `pressflow.aichieve.net`), no
  rule-7 deviation, and every number in this entry is a live probe or a named commit.

## Blockers for the sweep to surface

1. **[P0, founder, one paste]** The live key slot holds the key's **ID** (`mk_…`) — Stripe rejects it with 401
   (re-verified today; the sandbox key in the same env returns 200). Paste the real `sk_live_…` value and set
   `STRIPE_MODE=live`, or record practice mode as intended. 4 days to Day-30.
2. **[P0, founder, one reply]** Launch calls for **FacturGate, ParcelProof and CaseProof** —
   `@Simon approve launch FacturGate, ParcelProof, CaseProof`.
3. **[P0, owner decision]** Item 10 — the three public QuarterLine surfaces.
   `@Simon approve retire QuarterLine pages`.
4. **[P1, founder, one line]** Confirm the UPS AHS-Dimension trigger (96″ as published vs 48″ as intended) —
   9th day open.
5. **[P1, @Simon approve + owner migration]** Internal-traffic marker on `events` + a re-based gate window
   (item 5). The Growth Watchdog formally gates Day-30 on it.
6. **[P1, Simon / SOP]** Make the sweep's durable record non-optional (item 6); 11th day it survives only
   because a sweep run writes it.
7. **[P1, owner/editorial]** 38 social cards are ready and none is published; the oldest news (09-21…09-23) is
   aging out. Distribution is the one editorial step that is still approval-gated.
8. **[P2, @Simon approve, code]** The migration leftovers (item 11): repoint `ai-plugin.json` off the dead host
   and add a crawler map (`robots.txt` / `sitemap.xml`).
9. **[P2, editorial]** Five published articles are live but missing from the Supabase `articles` table.
10. **[P2, operator]** Restart the Hermes gateway when no cron run is in flight to load the fire-claim fix
    (doctor is clean; no visible symptom left). Avoid 09:30, 10:00 and 06:00–08:35.
11. **[P2, owner, if it was you]** The 09-25 00:36 deletion of four articles from the live site — the rebuild
    has restored them, so this is now only a guard-rail question.
