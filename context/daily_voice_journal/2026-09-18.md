# Voice Journal — 2026-09-18

Sweep run 08:00 UTC (Day-18; launch 2026-08-31; Day-14 scored an honest MISS on 09-14; Day-30 due ~09-30,
12 days out). Everything below marked *(live)* was re-probed in this run. Repo changes made this sweep: this
entry, the 09-18 growth-gate row, one playbook measurement addendum, and refreshed `STATUS.md` /
`pending_approval.md` lines — docs only, no `src/`.

## Shipped / changed since the 09-17 journal

**Nothing.** No commit has landed since `c1b3139` (09-17 03:26–03:49); `HEAD` is still `ffe0770` (the 09-17
sweep's own docs commit, 08:04). The build line has now been idle **28 h** (last product commit `e77db64`,
09-17 03:49), and its only queued product (ParcelProof, score 80) still has no go/no-go.

## Verified today (live, not journal-derived)

- Prod surfaces: `/` 200 · `/facturgate` 200 · **all 12** `/facturgate/calc/*` 200 (enumerated from
  `src/lib/seo/einvoice/presets.ts`, not sampled) · `/ledgerlink` 200 · `/quarterline` 200 · sampled
  quarterline pSEO (`california-freelancer-tax-2026`) 200 · `/pressflow` **404** (feature lives in
  `editorial-factory` behind auth) · `/api/articles.json` 404 on this app (same reason).
- `.well-known/mcp.json` *(live)*: 200, sha256 `f3b4cbd26198491de6125e3f28fba084ad568d90efbef619ccec0398bf327fea`
  — **byte-identical to the working tree**, v1.2.0. Retired tool name `calculate_self_employment_2026` →
  **HTTP 400** (rule 5 holds, second day).
- `node scripts/growth-check.mjs` *(live)* → `page_view=157, checkout_click=4, export_click=2,
  unique_sessions=54, unique_sessions_last_7d=46, session_instrumented_rows=58, quarterline_event_rows=163,
  session_id_column=true, charge_count=9, gross_revenue_usd=161, days_since_first_event=17`.
- Raw `events` *(live, full read)*: **374 rows** (quarterline 163 / factory 134 / ledgerlink 39 /
  facturgate 38), last write **2026-09-17 22:03:17Z**, first write 2026-09-01 19:18:36Z. **21 rows since the
  09-17 sweep's stamp**: 20 at 09-17 10:02:50–55 (Build Watchdog Playwright — facturgate 11, quarterline 4,
  factory 3, ledgerlink 2) and **1 outside every CI cluster** (see below). 149 distinct sessions total.
- **A 5th non-CI session appeared: `d8e77f23`, 1× `factory:page_view`, 2026-09-17 22:03:17Z.** The non-CI
  count has gone 2 (09-14) → 4 (09-17) → **5** (today), and it is the same shape as the last three:
  a single evening factory page view with a session id and nothing else. All 5 remain **unattributable**
  (no row stores a UA/referrer/path) → **0 provably-external sessions in 18 days live**. Evening clustering
  is now visible: `30eb9935` ended 09-16 23:57 and `d8e77f23` landed 09-17 22:03 — different sessions,
  ~22 h apart, both the directory page. Consistent with a human, still not evidence.
- **`agent_query` still 3, unchanged, all factory deploy smoke** (facturgate, 09-17 03:50:21–22,
  `validate_einvoice`×2 + `check_eu_vat_id`×1, all `session_id` null). **Organic agent queries: 0 on day 18.**
- New-in-table click types *(live)*: `validate_click=6`, `convert_click=6`, all `facturgate`, all inside the
  09-17 03:46–03:47 build smoke and the 10:02 Build Watchdog run — i.e. **all factory-generated**, and all
  invisible to `growth-check.mjs` (hard-scoped to `quarterline`). Another measurement that only exists in
  raw rows.
- **Production is still Stripe TEST MODE** *(live)*: `POST /api/checkout` → 200,
  `cs_test_a1nUEyl5GpvfML5VUnMn4IkGOCseA35hQLdKJRWAjZV45QsWPQ6riuYG8Y…`; DB: all **4** `purchases` rows
  `cs_test_*` ($9.00, all 2026-09-02); the single `subscriptions` row `canceled` (09-02).
  **Real collected revenue = $0.**
- `factory_config.distribution_queue` *(live)*: **36 items, all `ready`, 0 published, 0 deleted**,
  `updated_at` still **2026-09-12T14:48:22Z** — six days untouched. **The top three items are all the same
  story and its event date is TODAY, 2026-09-18** (`reddit:customs-enforcement-operational-risk:supplychain`,
  `…:logistics`, `linkedin:customs-enforcement-operational-risk` — "On Sept 18, a stale address can void your
  importer number…"). Q3 wedge expired 09-15 with nothing posted; this one expires in ~14 h.
- Crons *(live)*: 9 jobs active, **0 open incidents** (9 ledger rows, all `closed`). Today's 06:00
  `Full Pipeline: home_systems_reno` ran 06:07 **ok/completed** — no new fire-claim mislabel since 09-16.
  **Growth Watchdog fires today 17:00 UTC** (first run since 09-11) and is the scheduled scorer for the
  gate audit.

## Broke / risks

- **P0 — the distribution queue's own clock expires today, and this is the third wedge in a row to go
  unused.** 36/36 `ready`, 0 published, untouched since 09-12; item #1 is dated **2026-09-18**. This is still
  the only P0 needing no approval and no code, and it remains the reason every gate is unscoreable.
- **P0 — $0 real revenue, 12 days to the Day-30 gate.** Production has served test-mode checkout pages for
  18 days. The 09-16 `checkout_click` (session `30eb9935`) is still the only demand-side signal outside the
  09-02 window and it could not convert by construction.
- **P1 — the Growth Watchdog fires today at 17:00 onto a fully polluted metric set.** Its Day-14/D30 inputs
  are: `agent_query` = 3 (100 % factory deploy smoke), `unique_sessions` = 54 (100 % CI + unattributable),
  `gross_revenue` = $161 test-mode. The playbook's measurement rule is unambiguous, but this is the first
  run since 09-11 and the first time it will evaluate the Day-30 window (due ~09-30, a Wednesday — **no
  watchdog run that day**, so the daily sweep must score it on the date).
- **P1 — the unattributable-session pile keeps growing and every gate still reads it.** 2 → 4 → 5 in four
  days, no UA/referrer stored, and the internal-traffic marker (approval item 5) is still unbuilt; the same
  gap now also covers server-side `agent_query` rows.
- **P1 — build line idle 28 h with ParcelProof shortlisted and undecided.** FacturGate shipped as `beta`
  under an owner instruction; its own public-launch call is also still open.
- **P1 — durable-record step held for a third day, still non-structural.** Today's entry, gate row and queue
  header are written because this sweep writes them; `context/daily_voice_journal/` still has no producer.
- **P2 — the fire-claim fix is still unloaded** (patch in tree since 09-16; gateway pid `1963330` still the
  2026-09-12 15:13 process). Two jobs still display `error: Interrupted by shutdown` for 09-15/09-16 runs
  that in fact completed + delivered. No new mislabel on 09-17 or 09-18. Restart windows: ~09:00, after
  ~11:00 UTC, or before 17:00 (the Growth Watchdog window must not be interrupted).

## Founder or fleet notes

- `context/pending_approval.md` remains the authoritative open-work source; this journal is the daily audit.
  Item 3 closed; item 4 half-shipped (FacturGate beta live, ParcelProof undecided); items 1, 2, 5, 6, 7 open.
- Unchanged and still true: `/pressflow` 404s here, 9 crons active, 0 open incidents, no rule-7 deviation,
  `HEAD == origin/main`, tree clean.

## Blockers for the sweep to surface

1. **[P0, no approval needed, expires TODAY 14 h]** Echo: work `distribution_queue` — item #1's event date is
   **2026-09-18**. Six days untouched; the Q3 wedge already expired 09-15 unused.
2. **[P0, founder, ~5 min]** `sk_live` in the deploy env **or** record test-mode as intended. Day-30
   (≈09-30) inherits the same $0 otherwise.
3. **[P0, founder decision]** FacturGate's public launch call (`status: beta`) and **ParcelProof go/no-go** —
   the build line is idle 28 h.
4. **[P1, @Simon approve, code]** Internal-traffic marker on `events` **including server-side `agent_query`
   rows** + re-based gate window + `growth-check.mjs` extended beyond `quarterline` (LedgerLink's 39 rows and
   every facturgate `validate_click`/`convert_click` stay invisible to the gates).
5. **[P1, Simon / SOP]** Make the sweep's durable record non-optional: a producer for
   `context/daily_voice_journal/` or an explicit "write the entry + gate row + commit" step.
6. **[P1, operator, Hermes fork]** Load the fire-claim fix — restart `hermes_cli.main gateway run` while no
   cron run is in flight (mind the 17:00 Growth Watchdog).
7. **[P2, code]** FacturGate GTM surface: `?doc=` shareable scorecard, embeddable badge, web-tier Stripe
   products — all blocked on items 1–3 anyway.
