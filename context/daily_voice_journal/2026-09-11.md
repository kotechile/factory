# Voice Journal — 2026-09-11

Sweep run 08:00 UTC. Everything below is a live probe from this run, not journal-derived.

## Shipped / changed
- (none — no commits since `c6fcf11`; tree clean, `main` in sync with `origin/main`.)

## Verified today (live)
- `node scripts/growth-check.mjs` (08:0x UTC) →
  `unique_sessions=8`, `unique_sessions_last_7d=8`, `session_instrumented_rows=8`,
  `session_id_column=false`, `page_view=108` (quarterline), `checkout_click=3`, `export_click=2`,
  `charge_count=9`, `gross_revenue_usd=161`, `days_since_first_event=10`.
- **Every one of the 8 unique sessions is factory-generated.** Raw PostgREST read of `events`
  (214 rows total): every row with `id >= 194` (instrumentation start, 09-09 20:52) is a
  `page_view` carrying `payload.session_id`. Breakdown: 4 sessions = 09-09 deploy smoke
  (20:52:00–20:52:04), 4 sessions = the 09-10 10:01 Build Watchdog Playwright run, plus one
  repeat id (`3b3193ce…`, 3 hits 21:22 → 00:25). No event has been written since
  09-10 12:04 (~20 h).
- **All engagement ever recorded is one 42-minute window on 09-02:** the only non-`page_view`
  rows in the table are 3 `checkout_click` (01:57, 01:59, 02:02) and 2 `export_click` (02:39),
  all product `quarterline`. Nothing since. Revenue `$161` is unchanged from 09-02.
- **`agent_query` = 0 rows, all-time** (exact count probe). The `$0.25/query` agent tier has
  never been exercised by a real caller; the 09-10 verification row was deleted, so the metric is
  still honestly zero.
- **`.well-known/mcp.json` is byte-identical in the working tree and in production** (fetched
  live, 200): still advertises `calculate_self_employment_2026` (registered nowhere) and omits
  `calculate_qbi_deduction`, `calculate_quarterly_estimate`, `format_article_for_linkedin`,
  `reconcile_stripe_payout`. Dead name is still live.
- Build Watchdog ran 09-10 10:01 `ok`; next run today 10:00. No fire-claim TTL recurrence.

## Broke / risks
- **Day-7 gate verdict: MISSED — and the metric is self-referential.** 8 unique sessions vs the
  ≥50 criterion, and all 8 are our own smoke/QA traffic. The gate as instrumented can be
  "improved" by our own Playwright runs, so the fallback trigger (pSEO expansion) is being
  justified by a number that does not measure the market. Export criterion met on paper
  (2 `export_click` + 9 charges) but those rows are 9 days old.
- **Migration `0002_events_session_id.sql` still NOT applied.** `session_id_column: false` (42703
  path still taken). This sweep confirmed the service-role key cannot fix it: PostgREST has no
  DDL path and `rpc/exec_sql` → 404. It must be applied in the Supabase SQL editor. Not
  data-loss (payload fallback works, 8 rows readable) but the indexed column is absent and every
  gate query stays a full-scan jsonb extraction.
- **pSEO fallback is unprepared and the gap is bigger than recorded.** `src/lib/seo/presets.ts`
  holds **9** presets (`california-freelancer-tax-2026`, `new-york-schedule-c-qbi`,
  `texas-1099-estimated-tax`, `consulting-sstb-phaseout-2026`, `single-member-llc-safe-harbor`,
  `therapist-qbi-deduction`, `software-engineer-1099-writeoffs`, `150k-schedule-c-taxes`,
  `250k-qbi-phaseout-calculator`). The 09-10 record says "holds 11 → 9 short"; the real gap to the
  journal's 20-route fallback is **11 short**. The playbook itself is inconsistent: §4's
  human-in-the-loop table says "Deploy 50 programmatic SEO landing pages" while the Day-7 line
  says "pSEO expansion". The route (`src/app/quarterline/calc/[slug]/page.tsx`) is generic, so
  authoring is preset-data only.
- **Echo outreach: still zero execution record, 4 days to the Sept 15 deadline.** Only artifact is
  the blueprint (`context/growth_blueprints/2026-08-31_quarterline.md`). Approved 09-09, never
  dispatched. The deadline is the product's entire urgency moat and it expires regardless of gate
  state.
- Journal has no automated producer (`README.md` still says entries are manual) — entries exist
  only because sweeps write them; a skipped sweep = a silent audit gap (09-07/09-08 were skipped).

## Founder or fleet notes
- `context/pending_approval.md` remains the authoritative open-work source; this journal is the
  daily audit. Today's live state and the corrected preset count are written back into it.

## Blockers for the sweep to surface
- **[P0, expires today 17:00]** Growth Watchdog (Fri 17:00) will evaluate a gate that cannot pass
  and cannot be honestly re-based until: (a) internal/QA sessions are tagged or excluded, and
  (b) the gate window is re-based to instrumentation start (09-09 20:52). Applying migration 0002
  is no longer verdict-blocking (payload fallback works) — it is a performance/index fix.
- **[P1, approval needed]** Author the missing pSEO presets (11 to reach the journal's 20 target)
  so the fallback can fire the moment the gate verdict is logged. Code change → needs
  `@Simon approve`.
- **[P0, approval needed]** Replace hand-written `public/.well-known/mcp.json` with a manifest
  generated from `src/products/registry.ts` + a consistency test (registry ↔ registered tool
  names ↔ manifest). Still unapproved since 09-10.
- **[P1, no approval needed]** Echo: execute GTM Vectors 1 & 5 now — distribution only.
