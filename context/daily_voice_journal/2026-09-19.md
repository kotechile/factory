# Voice Journal — 2026-09-19

Sweep run 08:00 UTC (Day-19; launch 2026-08-31; Day-14 scored an honest MISS on 09-14; Day-30 due ~09-30,
11 days out — a Wednesday, so the daily sweep scores it). Everything below marked *(live)* was re-probed in
this run. Repo changes made this sweep: this entry, the 09-19 growth-gate row, and refreshed `STATUS.md` /
`pending_approval.md` lines — docs only, no `src/`.

## Shipped / changed since the 09-18 journal

Three commits, all 2026-09-18 21:32–22:07 UTC — **the build line woke and shipped ParcelProof**:

- `73cad11` docs: **ParcelProof approved by owner instruction 2026-09-18** ("approve ParcelProof"),
  v1 scope guard recorded; build dispatched as a one-shot cron job.
- `3584a62` feat(parcelproof): **ParcelProof v1 beta shipped** — the DIM-weight / surcharge audit engine
  (`src/lib/calc/parcelaudit/`, 32 known-answer vitest vectors), the paste/upload recovery UI + dispute
  CSV, 6 `/parcelproof/calc/*` preset pages, and the two WebMCP tools `audit_carrier_invoice` +
  `compute_billable_weight`; `verify-build.sh` green end to end (92 vitest, 28 Playwright, Gemini
  visual-QA PASS ×3) before the push.
- `19d9529` docs: queue item 4's ParcelProof half recorded as shipped (the approval-queue header).
- `0d72585` chore: the **Growth Watchdog's 09-18 17:00 run** logged its gate row (first run since 09-11).

`HEAD == origin/main == 19d9529`, tree clean. The 42 h build-line idle that the 09-18 sweep reported ended
with this ship; the line is idle again since 09-18 22:05 (~10 h).

## Verified today (live, not journal-derived)

- Prod surfaces *(live)*: `/` 200 · `/quarterline` 200 · `/ledgerlink` 200 · `/facturgate` 200 ·
  **`/parcelproof` 200** · **all 6** `/parcelproof/calc/*` 200 (slugs enumerated from
  `src/lib/seo/parcelaudit/presets.ts`, not sampled) · `/pressflow` still 404 here (the feature lives in
  `editorial-factory` behind auth).
- `.well-known/mcp.json` *(live)*: 200, **v1.3.0, 8 tools**, sha256
  `6ea29ed1a0a1bac9daf3f9ccf1e4dd4de01a2e4934d4f29b7f4878e37ee2d9c9` — **byte-identical to the working
  tree**. The two new ParcelProof tools are advertised with real schemas. Retired tool name
  `calculate_self_employment_2026` → **HTTP 400** (rule 5 holds, third day).
- `node scripts/growth-check.mjs` *(live)* → `page_view=190` (+29), `checkout_click=4`, `export_click=2`,
  `unique_sessions=87` (+29), `unique_sessions_last_7d=72`, `session_instrumented_rows=91`,
  `quarterline_event_rows=196` (+29), `session_id_column=true`, `charge_count=9`, `gross_revenue_usd=161`,
  `days_since_first_event=18`.
- Raw `events` *(live, full read)*: **633 rows** (quarterline 196 / factory 152 / **parcelproof 132** /
  facturgate 105 / ledgerlink 48), last write **2026-09-18 22:07:10Z**, first 2026-09-01 19:18:36Z;
  328 distinct session ids. **The +259 rows since the 09-18 17:00 watchdog are entirely CI**: the 10:02
  Build Watchdog run and the ParcelProof verify-build burst (21:55 → 22:05, 281 rows including product
  Playwright). Nothing has been written since 22:07:10Z.
- **`agent_query` is now 4, all factory deploy smoke**: `validate_einvoice` ×2 + `check_eu_vat_id`
  (09-17 03:50, FacturGate) and **`audit_carrier_invoice` (09-18 22:07:10, ParcelProof — this ship's own
  post-deploy probe)**, all `session_id` null. **Organic agent queries: 0 on day 19.**
- **Non-CI sessions unchanged at 5** (`3b3193ce`, `30eb9935`, `3f26bb8f`, `49d8211a`, `d8e77f23`); none
  stores a UA/referrer/path, so all five stay unattributable → **0 provably-external sessions in 19 days
  live**. No new unattributable session appeared overnight.
- **Production is still Stripe TEST MODE** *(live)*: `POST /api/checkout` → 200,
  `cs_test_a1LStiwlH2DBLwFSrfgScJQ8IZBF576giZbl6s0BnF1rygt6EqMPd5sIIF`; all **4** `purchases` rows
  `cs_test_*` ($9.00, all 2026-09-02); the single `subscriptions` row `canceled` (09-02).
  **Real collected revenue = $0.**
- `factory_config.distribution_queue` *(live)*: **36 items, all `ready`, 0 published, 0 deleted**,
  `updated_at` still **2026-09-12T14:48:22.64Z** — **seven days untouched**. **Item #1's event date
  (2026-09-18) has now passed with nothing posted** (top three: `reddit:customs-enforcement-operational-risk:
  supplychain`, `…:logistics`, `linkedin:customs-enforcement-operational-risk`). Third wedge in a row to
  expire unused (Q3 estimated-tax wedge 09-15, this one 09-18).
- Crons *(live)*: 9 jobs active, **0 open incidents** (9 ledger rows, all `closed`). Every job's last run
  is `ok`/`completed`+`delivered` except the two historical fire-claim mislabels (09-15, 09-16). No new
  mislabel on 09-17, 09-18 or 09-19.
- **Growth Watchdog 09-18 17:00 ran as scheduled** *(ledger + commit `0d72585`)* and logged a gate row:
  Day-7 and Day-14 MISS held, Day-30 not yet due, and **no fallback was fireable** (the Day-7 pSEO
  fallback already shipped; the Day-14 A/B copy test is blocked-on-traffic with 0 external visitors to
  split). It explicitly gated Day-30 on the internal-traffic marker (queue item 5) — still unbuilt.

## Broke / risks

- **P0 — the distribution queue's lead wedge expired unpublished, and the queue has now been untouched
  7 days.** 36/36 `ready`, 0 published since 09-12 14:48. This remains the only P0 needing no approval
  and no code, and it is the reason every gate stays unscoreable.
- **P0 — $0 real revenue, 11 days to the Day-30 gate.** Production has served sandbox checkout pages for
  19 days; the 09-16 demand-side click could not convert by construction. Day-30 (≈09-30) inherits $0.
- **P1 — the agent leg of the gate is now 4/4 factory smoke.** The ParcelProof ship added a fourth
  self-generated `agent_query` row to the exact metric the Day-30 gate reads (Day-30 criterion:
  ≥100 agent queries). Organic agent demand is 0 on day 19.
- **P1 — both shipped products sit at `beta` with their public launch call unanswered** (FacturGate since
  09-17, ParcelProof since 09-18). Nothing can reach them while they are unlaunched, so the 32 indexable
  routes still rest on traffic that is 100 % factory-generated.
- **P1 — one open correctness question on the shipped ParcelProof** (from the ship note): the tariff table
  makes UPS's AHS-Dimension trigger a longest side > 96″ while FedEx triggers at > 48″, so a 40″ parcel is
  ineligible at *both* carriers. If the intended UPS trigger is 48″, that is a one-line change in
  `src/lib/calc/parcelaudit/surcharges.ts` and the vectors follow.
- **P1 — the internal-traffic marker (queue item 5) is still unbuilt**, and the Growth Watchdog has now
  formally recorded that Day-30 cannot be scored without it. Day-30 is the last scheduled gate.
- **P1 — durable-record step held for a fourth day, still non-structural.** Today's entry and gate row are
  written because this sweep writes them; `context/daily_voice_journal/` still has no producer.
- **P2 — the fire-claim fix is still unloaded** (patch in tree since 09-16; gateway pid `1963330` still the
  2026-09-12 15:13 process). Two jobs still display `error: Interrupted by shutdown …` for 09-15/09-16 runs
  that in fact completed + delivered. No new mislabel in three days. Restart windows: ~09:00, after
  ~11:00, or before 17:00 UTC — never while a cron run is in flight.

## Founder or fleet notes

- `context/pending_approval.md` remains the authoritative open-work source; this journal is the daily
  audit. Item 3 closed (09-17); item 4 is now **fully shipped** (FacturGate `beta` + ParcelProof `beta`,
  both unlaunched); items 1, 2, 5, 6, 7 open.
- Unchanged and still true: `/pressflow` 404s here, 9 crons active, 0 open incidents, no rule-7 deviation,
  `HEAD == origin/main`, tree clean.

## Blockers for the sweep to surface

1. **[P0, founder, ~5 min]** `sk_live` in the deploy env **or** record sandbox as intended. Day-30
   (≈09-30, 11 days) inherits the same $0 otherwise.
2. **[P0, no approval needed, expired 09-18]** Echo: work `distribution_queue` — 36/36 `ready`, 0
   published, seven days untouched, lead item's date now past.
3. **[P0, founder decision]** Public launch calls for **FacturGate and ParcelProof** (both `beta`) —
   nothing can reach either product until they are launched.
4. **[P1, @Simon approve, code]** Internal-traffic marker on `events` **including server-side
   `agent_query` rows** + re-based gate window + `growth-check.mjs` extended beyond `quarterline` — the
   Growth Watchdog has formally recorded that Day-30 cannot be scored without it.
5. **[P1, founder, one-line]** Confirm the UPS AHS-Dimension trigger (96″ as published vs 48″ as
   intended) in `src/lib/calc/parcelaudit/surcharges.ts`.
6. **[P1, Simon / SOP]** Make the sweep's durable record non-optional: a producer for
   `context/daily_voice_journal/` or an explicit "write the entry + gate row + commit" step.
7. **[P1, operator, Hermes fork]** Load the fire-claim fix — restart `hermes_cli.main gateway run` while no
   cron run is in flight.
