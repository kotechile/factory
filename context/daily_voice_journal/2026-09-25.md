# Voice Journal — 2026-09-25

Sweep run 08:00 UTC (Day-25; launch 2026-08-31; Day-14 scored an honest MISS on 09-14; Day-30 due
**2026-09-30 — 5 days out**, a Wednesday, so the daily sweep scores it and no watchdog run exists that day; the
Growth Watchdog runs **tonight 09-25 17:00**, the last watchdog before the gate). Everything below marked *(live)*
was re-probed in this run. Repo changes made this sweep: this entry and refreshed `STATUS.md` /
`pending_approval.md` header lines — docs only, no `src/`.

## Shipped / changed since the 09-24 journal

**Nothing was built or shipped.** `HEAD == origin/main == 7cef2d8` (the 09-24 sweep's own docs commit) and the
running app image is `af8yqbwrrnyyfgs9wcg0intj:7cef2d86fc81…` *(live, `docker ps`, up 24 h)*, i.e. that docs
commit itself. No commit in the factory repo in 24 h; the build line has now been idle since the two 09-22 builds
(≈70 h).

Five measurable changes, none from a factory build:

- **P0 — four of the six published articles vanished from the live editorial site overnight, and nothing in any
  repo explains it.** `/api/articles.json` on `pressflow.aichieve.net` returns **2** entries now, not 6 *(live)*;
  inside the running pressflow container `published/` has **only 2 files and a directory mtime of 2026-09-25
  00:36** *(live, `docker exec ls -la`)*, though the image was built 09-23 13:15 with all six. Supabase `articles`
  now holds **2 rows** *(live, service-role count)*, both created 09-23 13:01/13:02. Gone: `nvidia-hugging-face-moat`,
  `rollback-breaks-execution-continuity`, `temporal-durable-execution-12-55b`,
  `unfi-warehouse-automation-capex`. The fingerprint of the **dashboard's own article-delete path** is exact: that
  handler unlinks the files, deletes the Supabase rows, strips `published_log` lines and calls
  `cleanQueueForArticle`, which rewrites the queue — and `factory_config.distribution_queue` carries `updated_at`
  **2026-09-25T00:36:44Z** *(live)*. Not attributable from the server side: traefik has no access logging, the
  pressflow container logs only its startup banner, and `/root/.hermes/logs/agent.log` shows only gateway
  housekeeping at that hour (no session, no tool call). **The factory repo is untouched and is now inconsistent with
  live**: `/root/editorial-factory` still carries all six files in `published/`, `context/published_log.md` still
  lists all six as published, and `origin/main == 2c6f89f` — so the next rebuild re-bakes all six and resurrects the
  four.
- **The social queue lost the nine posts that belonged to those four articles: 15 → 6 items** *(live)*, all `ready`,
  **0 published**. Remaining: `mcp-skills-extension` (r/AI_Agents, r/LocalLLaMA, LinkedIn) and
  `ai-price-volatility-build-vs-buy-maintenance-tax` (r/ExperiencedDevs, r/devops, LinkedIn). The underlying news
  is 09-21…09-23 and ages out within days.
- **Traffic: 62 new rows, all of them our own test run, and none since.** `events` **1485 → 1547 rows** *(live,
  full read)*; last event **2026-09-24T10:01:37Z** → **22 h with no event at all**. The 62 landed 09-24
  10:01:22 → 10:01:37Z: the daily Build Watchdog's Playwright run (caseproof / facturgate / parcelproof /
  ledgerlink / factory). Distinct sessions 899 → **940**, i.e. every new session id is that run.
- **`product=caseproof` 137 → 159 rows** *(live)*, all of it the same 10:01 test run. Fifth consecutive day this
  number is our own verification traffic.
- **`quarterline` 7-day unique sessions 69 → 65** *(live, `growth-check.mjs`)* — the window rolled forward past the
  09-17/09-18 cluster and nothing replaced it. Still sandbox: `stripe_mode "test"`, `revenue_is_sandbox true`,
  `gross_revenue_usd 161`, `charge_count 9`.

## Verified today (live, not journal-derived)

- Prod surfaces on `apps.giniloh.com` *(live)*: `/` **307** → `https://giniloh.com` · `/showcase`, `/ledgerlink`,
  `/facturgate`, `/parcelproof`, `/caseproof`, `/quarterline`, `/embed/countdown` all **200** · legacy
  `factory.aichieve.net` still **503**. No regression.
- **All 20 `/quarterline/calc/*` presets 200** *(live, every slug read out of `src/lib/seo/presets.ts` and probed
  one by one)*; the full preset inventory is still 44 (20 quarterline + 12 facturgate + 6 parcelproof + 6 caseproof,
  counted from source). **Item 10 therefore unchanged**: the retired product's calculator page, its 20 indexable
  landing pages and `/embed/countdown` are all still public.
- Manifest *(live)*: `/.well-known/mcp.json` **200, v1.5.0, 9 tools**, sha256
  `2843f352f28fbf1adae82c9fd6ad3808932f99e23043e210c4aa0853acf232ea` — **byte-identical** to the tree.
- Paid surface *(live, POST)*: `{plan:pdf_audit_export, app:ledgerlink}` → **200** `cs_test_a1omovzY…` ·
  `app=caseproof` → **200** `cs_test_a1R4YkCg…` · app-less → **400** naming the inventory · `app=quarterline` →
  **400** "retired and cannot be purchased". `/api/portal` → **307** `?error=no_customer_found`. **Item 8 holds.**
- **P0 — the live payment key slot still holds a key ID, and Stripe still rejects it.** Read-only
  `GET https://api.stripe.com/v1/balance` with the deployed value → **HTTP 401**, verbatim: *"Invalid API key
  provided: mk_1UAeG***************wbFj. This looks like the ID of an API key rather than the key itself."* The test
  key in the same env returns **200** (the sandbox works). Deploy env unchanged: `STRIPE_MODE=test`,
  `STRIPE_SECRET_KEY_TEST=sk_test_…`, `STRIPE_WEBHOOK_SECRET_TEST=whsec_g6Jc…`,
  `STRIPE_SECRET_KEY_LIVE=mk_1UAeGxJaTDc3aAp0lG3UwbFj`, `STRIPE_WEBHOOK_SECRET_LIVE=whsec_vn7B…`. Day-30 inherits
  $0 until the real `sk_live_…` value is pasted.
- Raw `events` *(live, full read)*: **1547 rows** (parcelproof 478 / facturgate 321 / factory 228 / quarterline 228
  / caseproof 159 / ledgerlink 133), **940 distinct session ids**, 198 null-session rows, 4 `agent_query`.
- **`agent_query` is still 4** — facturgate ×3 (09-17 03:50) + parcelproof ×1 (09-18 22:07), every one a factory
  ship probe. **Organic agent queries: 0 on day 25.** Day-30's criterion is ≥100.
- **No real revenue** *(live, API read-back + DB)*: 4 `purchases` rows all `cs_test_*` ($9.00, all 2026-09-02); the
  single `subscriptions` row `canceled`. **$0 collected, 25 days in.**
- Editorial fleet *(live)*: pressflow `/healthz` **200**, container image `2c6f89f…` == its HEAD, 43 h uptime;
  factory crons **30 active, `hermes cron doctor` clean** (no issues, no stale labels); today's editorial runs —
  `resilient_home_assets` 06:07:13, `career_velocity_equity_engine` 06:37:22, `last_mile_routing_fleet_carbo`
  07:10:30 — all `ok`. Next Growth Watchdog: **09-25 17:00**.
- `node scripts/vertical-sync.mjs --check` *(live)* → exit 0, "26 verticals, no drift (vendored)".
- **Domain-migration leftovers unchanged** *(live)*: `/.well-known/ai-plugin.json` 200 with `api.url`
  `https://factory.aichieve.net/openapi.json` and the dead host in `legal_info_url`; `/openapi.json`, `/robots.txt`
  and `/sitemap.xml` all **404** with no producer, for 44 indexable landing pages.
- Repo *(live)*: `HEAD == origin/main == 7cef2d8`, tree clean except the pre-existing untracked `.hermes/`.

## Broke / risks

- **P0 — NEW: a live editorial surface lost four published articles at 00:36 UTC and the repo still carries them
  (see above).** Either an owner-side deliberate delete (then the repo/record must catch up, or the next deploy
  brings them back) or an unexplained deletion worth investigating. The factory has no logging that can attribute
  it.
- **P0 — the live key slot is still wrong, now 5 days from Day-30.** Re-measured, not inferred. One paste of the real
  `sk_live_…` (+ `STRIPE_MODE=live`) is the whole fix.
- **P0 — three finished products are still invisible to the public** (all `beta`): FacturGate since 09-17 (8 days),
  ParcelProof since 09-18 (7 days), CaseProof since 09-22 (3 days). No commit touched their status.
- **P0 — the retirement still leaves three QuarterLine surfaces public (item 10).** Re-verified today end to end:
  `/quarterline` 200 with a working calculator whose $9 export returns an explicit 400, all 20 preset pages 200,
  `/embed/countdown` 200.
- **P1 — the Day-30 agent leg is 4/4 factory smoke** vs the ≥100 criterion; organic agent demand 0, fifth day
  unchanged.
- **P1 — the internal-traffic marker (item 5) is still unbuilt.** Today adds the same evidence as yesterday: 62 of 62
  new rows were ours, so every growth gate reads numbers we generate.
- **P1 — the ParcelProof correctness question is now 8 days open** (UPS AHS-Dimension trigger 96″ as published vs 48″
  as intended; one line in `src/lib/calc/parcelaudit/surcharges.ts`).
- **P1 — the durable-record step held for a tenth day, still non-structural** (item 6): today's entry exists because
  this sweep writes it. `context/daily_voice_journal/` still has no producer.
- **P2 — item 11 leftovers** (dead-host descriptor + no crawler map) unchanged; **item 7** (gateway restart, no
  visible symptom) unchanged; cron doctor is clean, so nothing is mislabelled.
- **Observation, continuing:** the written-and-waiting social queue is down to 6 posts from 15, none published, and
  its news window (09-21…09-23) is expiring.

## Founder or fleet notes

- `context/pending_approval.md` remains the authoritative open-work source; this journal is the daily audit. Items
  2, 3, 4, 8 and 9 are closed; **open: 1 (live key), 5 (traffic marker), 6 (durable record), 7 (gateway restart,
  P2), 10 (QuarterLine surfaces), 11 (migration leftovers)** — plus the three launch calls, the ParcelProof
  one-liner, and today's new article-deletion question.
- Every queue lead is still an owner decision: one paste, three launch approvals, one publish/retire approval, one
  data confirmation, one delete confirmation. The fleet has nothing buildable ahead of those.
- Unchanged and still true: `/pressflow` 404s on the factory host (it lives at `pressflow.aichieve.net`), no rule-7
  deviation, and every number in this entry is a live probe or a named commit.

## Blockers for the sweep to surface

1. **[P0, founder, one paste]** The live key slot holds the key's **ID** (`mk_…`) — Stripe rejects it with 401
   (re-verified today; the sandbox key in the same env returns 200). Paste the real `sk_live_…` value and set
   `STRIPE_MODE=live`, or record practice mode as intended. 5 days to Day-30.
2. **[P0, founder, one reply]** Launch calls for **FacturGate, ParcelProof and CaseProof** —
   `@Simon approve launch FacturGate, ParcelProof, CaseProof`.
3. **[P0, NEW, founder, one reply]** Four published articles + nine queued social posts disappeared from the live
   editorial site at 00:36 UTC today; the factory repo still carries the articles and the published record, so a
   rebuild redeploys them. Confirm whether the delete was yours: if yes, the repo must catch up; if no, it needs an
   investigation.
4. **[P0, owner decision]** Item 10 — the three public QuarterLine surfaces.
   `@Simon approve retire QuarterLine pages`.
5. **[P1, founder, one line]** Confirm the UPS AHS-Dimension trigger (96″ as published vs 48″ as intended) — 8th day
   open.
6. **[P1, @Simon approve + owner migration]** Internal-traffic marker on `events` + a re-based gate window (item 5).
   The Growth Watchdog formally gates Day-30 on it.
7. **[P1, Simon / SOP]** Make the sweep's durable record non-optional (item 6); 10th day it survives only because a
   sweep run writes it.
8. **[P2, @Simon approve, code]** The migration leftovers (item 11): repoint `ai-plugin.json` off the dead host and
   add a crawler map (`robots.txt` / `sitemap.xml`).
9. **[P2, operator]** Restart the Hermes gateway when no cron run is in flight to load the fire-claim fix (doctor is
   clean; no visible symptom left). Do not do it 17:00 today, or 06:00–08:30 while the editorial jobs run.
10. **[P2, owner/editorial]** 6 social posts are ready and none is published; the underlying news is 09-21…09-23 and
    will age out within days.
